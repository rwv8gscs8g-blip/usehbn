---
path: .hbn/results/20260621-091000-grok-cross-ia-w-orq-4d-fix2-0082.md
id-global: 20260621-091000-grok-cross-ia-w-orq-4d-fix2-0082
tipo: audit-result
autor: grok
familia: xAI
veredito: "APROVA_0082: SIM"
arvore: fronteira
created_at: "2026-06-21T09:10:00-03:00"
---

SOU: grok · familia xAI · papel auditor

VERIFICACOES OBRIGATORIAS (comando+saida e arquivo:linha; Truth Barrier = disco):

1. git rev-parse main (main INTOCADA):
$ cd /Users/macbookpro/Projetos/usehbn && git rev-parse main
4db692876381a0d7909985c8500d999f2e677b04
$ git log -1 --format="%H %s" main
4db692876381a0d7909985c8500d999f2e677b04 docs: emenda plano hbn-exuvia v2
(HEAD atual da branch proponente: 9ac23f2... mas main permanece intacta conforme k-0025)

2. guards/ci-entry.sh (set -e + 3 invocacoes reais):
```2:5:guards/ci-entry.sh
set -euo pipefail
bash guards/hbn-guards-runner.sh
bash guards/tests/run-guard-tests.sh
bash guards/tests/adversarial-battery.sh
```
$ cat guards/ci-entry.sh
#!/usr/bin/env bash
set -euo pipefail
bash guards/hbn-guards-runner.sh
bash guards/tests/run-guard-tests.sh
bash guards/tests/adversarial-battery.sh

3. .github/workflows/hbn-shield.yml (step run EXATO + YAML):
```29:29:.github/workflows/hbn-shield.yml
        run: bash guards/ci-entry.sh
```
$ grep -n 'run: bash guards/ci-entry.sh' .github/workflows/hbn-shield.yml
29:        run: bash guards/ci-entry.sh
O job "guards" possui exatamente UM step com esse run (os demais runs estao no job "tests", que e separado). Estrutura YAML simples e valida (on, jobs, steps com uses + run + env).

4. guards/assert-ci-battery.sh (A igualdade exata + B invocacoes reais heredoc-aware):
```66:72:guards/assert-ci-battery.sh
workflow_has_exact_entrypoint() {
    WORKFLOW_CONTENT="$CONTENT" python3 - <<'PY'
...
    target = "bash guards/ci-entry.sh"
```
```195:202:guards/assert-ci-battery.sh
if ! workflow_has_exact_entrypoint; then
    guard_fail "${WORKFLOW_PATH} nao contem step run exatamente igual a 'bash guards/ci-entry.sh' no indice/HEAD."
    exit 1
fi

if ! entrypoint_has_real_invocations; then
    guard_fail "${ENTRYPOINT_PATH} nao invoca runner, suite e bateria como comandos reais fora de heredoc/comentario/echo, ou nao habilita set -e."
    exit 1
fi
```
```124:141:guards/assert-ci-battery.sh
entrypoint_has_real_invocations() {
    ...
    required = [
        "guards/hbn-guards-runner.sh",
        "guards/tests/run-guard-tests.sh",
        "guards/tests/adversarial-battery.sh",
    ]
...
    def strip_shell_comment(line):
        ...
    def heredoc_marker(line):
        ...
    for raw in lines:
        ...
        if re.match(r"^bash[ \t]+" + re.escape(path) + r"([ \t]|$)", clean):
            seen[path] = True
        marker = heredoc_marker(clean)
        if marker is not None:
            skip_marker, strip_tabs = marker
```
Linhas-chave: deteccao de set -e (linhas ~172), skip de heredoc (156-161), match de bash real (176), strip de comentarios shell/yaml, unquote, block handling para | >.

5. ATAQUE em fixture descartavel (/tmp/hbn-cross-audit-0082-tester.py, stdlib apenas, sem tocar repo):
$ python3 /tmp/hbn-cross-audit-0082-tester.py
=== TESTES DE ATAQUE (fixture descartavel) ===
BOM (inline exato + reais): wf=True ep=True => PASSOU (esperado: PASS (bom)) ✓
(a) run: echo "...": wf=False ep=True => BLOQUEOU (esperado: BLOQUEIA (ruim)) ✓
(b) heredoc-data no run (cat << com string): wf=False ep=True => BLOQUEOU (esperado: BLOQUEIA (ruim)) ✓
(c) comentario inline escondendo string: wf=False ep=True => BLOQUEOU (esperado: BLOQUEIA (ruim)) ✓
(d) sem step run exato (invoca 3 diretamente): wf=False ep=True => BLOQUEOU (esperado: BLOQUEIA (ruim)) ✓
ancora yaml (run: &ci cmd): wf=False ep=True => BLOQUEOU (esperado: BLOQUEIA (ruim)) ✓
quoted single ' ' (deve passar unquote): wf=True ep=True => PASSOU (esperado: PASS (bom)) ✓
espacos duplos (nao deve normalizar para match): wf=False ep=True => BLOQUEOU (esperado: BLOQUEIA (ruim)) ✓
ci-entry com invocacao em heredoc: wf=True ep=False => BLOQUEOU (esperado: BLOQUEIA (ruim)) ✓
ci-entry com echo da bateria: wf=True ep=False => BLOQUEOU (esperado: BLOQUEIA (ruim)) ✓
ci-entry com comentario na bateria: wf=True ep=False => BLOQUEOU (esperado: BLOQUEIA (ruim)) ✓
=== FIM DOS TESTES ===
TODOS OS CASOS COMO ESPERADO: SIM
(Confirmado: todas as 4 burlas + extras de ancora/espaco/aspas/heredoc-in-sh BLOQUEIAM; BOM passa. Parser exige igualdade EXATA no scalar run: + invocacao real fora de heredoc/echo/comentario.)

6. Execucao dos runners (verde):
$ bash guards/hbn-guards-runner.sh
...
[hbn-guards/assert-ci-battery] ✓ CI preserva entrypoint canonico e invocacoes reais de runner, suite e bateria.
...
[hbn-guards] Todos os guards passaram.

$ bash guards/tests/run-guard-tests.sh
...
== assert-ci-battery (G-CI-BATTERY) ==
  ✓ ci-battery: workflow com entrypoint exato passa (esperado: pass)
  ✓ ci-battery: workflow sem step exato de ci-entry.sh -> BLOCK (esperado: block)
  ✓ ci-battery: workflow com echo de ci-entry.sh -> BLOCK (esperado: block)
  ✓ ci-battery: workflow com heredoc-data de ci-entry.sh -> BLOCK (esperado: block)
  ...
== resumo: 257 passaram, 0 falharam ==
SUÍTE VERDE — todos os casos-ruins BLOQUEADOS, todos os casos-bons passam.

$ bash guards/tests/adversarial-battery.sh
...
B85 CI comentario inline ci-entry.sh                 | G-CI     | BLOQUEADA ✓
B86 CI echo ci-entry.sh                              | G-CI     | BLOQUEADA ✓
B87 CI heredoc-data ci-entry.sh                      | G-CI     | BLOQUEADA ✓
...
BATERIA VERDE — toda burla documentada foi BLOQUEADA pelo guard correspondente.

7. readback 0082:
```7:8:.hbn/readbacks/0082-w-orq-4d-fix2-ci-entry.json
  "status": "implemented_pending_cross_audit",
  "activation_status": "PROPOSED_UNTIL_CROSS_AUDIT",
```
Escopo (do json): files_allowed incluem .github/workflows/hbn-shield.yml, guards/ci-entry.sh, guards/assert-ci-battery.sh, guards/tests/*, .hbn/... ; files_forbidden incluem main, core/**, guards/data/**, guards/hbn-guards-runner.sh .
$ git show 7932f552b60255a94061f55c2156359ae368913d --name-only | cat
...
.github/workflows/hbn-shield.yml
...
guards/assert-ci-battery.sh
guards/ci-entry.sh
guards/tests/adversarial-battery.sh
guards/tests/run-guard-tests.sh
.hbn/...
(Commit 7932f55 carrega HBN-Readback: 0082; sem read-list-canonica, sem orchestrator-profile-spec, sem guards/data/*, sem hbn-guards-runner.sh. Escopo respeitado. read-list, guards/data, orchestrator-profile-spec, runner NAO tocados nesta mudanca.)

8. Busca por nova burla a igualdade exata (anchors, multiplos run, normalizacao espacos, aspas, etc):
- Ancora yaml (run: &ci bash... + possivel *ci): wf=False (cmd capturado inclui "&ci ") => BLOQUEIA (testado no fixture).
- Multiplos run: parser coleta TODOS via any==target; presenca do exato + outros steps (como no yml real com job tests) => PASS correto; ausencia do exato => BLOCK.
- Normalizacao espacos: "bash  guards/..." (duplo) apos .strip() ainda tem espaco duplo, != target => BLOQUEIA (fixture confirmou; nao ha over-strip de inner ws).
- Aspas: 'bash ...' e "bash ..." => unquote_scalar remove => PASS (bom); sem aspas ou misturadas nao match.
- Outros (run com | block contendo string como dado, inline # apos echo, etc): ja cobertos em B85-B87 e fixture; nenhum passa.
Nenhuma nova burla encontrada. O parser (regex + strip + heredoc-skip + unquote + shlex) e suficiente para igualdade exata + deteccao real.

CONCLUSAO DA AUDITORIA CRUZADA (familia xAI, chat novo, read-only, sem memoria previa, sem alteracao de versionados):
- Design por IGUALDADE EXATA implementado corretamente.
- G-CI-BATTERY agora valida (A) literal exato no workflow (nao substring) e (B) chamadas reais no ci-entry (heredoc-aware, set -e obrigatorio).
- Todos os casos de ataque das versoes anteriores e novos probes BLOQUEIAM.
- Runners verdes, bateria adversarial verde (B85/B86/B87 incluidas), escopo do readback 0082 respeitado, main intacta.
- Runner G-EXC/SIGPIPE selado em 0084 (conforme contexto).

APROVA_0082: SIM
