---
titulo: "Parecer Codex — re-auditoria da onda 0006 v3"
tipo: result
path: .hbn/results/20260613-105957-codex-reaudit-onda-0006-v3.md
id-global: 20260613-105957-codex-reaudit-onda-0006-v3
temperatura: frio
auditor: codex-openai
alvo: "delta v2 -> v3 da branch proposta/onda-0006-v3, 5 picks pendentes sobre 8f7f175"
reviewed_at: "2026-06-13T10:59:57-03:00"
ambiente_execucao: "Codex/sandbox informativo; nao e Terminal do operador"
---

# PAPEL/BASTAO/CONTEXTO

Auditor cruzado de trabalho de outra IA (corretor Fable 5). Nao implementei
mudancas de produto nem de guard; a unica escrita persistente deste turno e
este parecer. Raiz canonica auditada: `/Users/macbookpro/Projetos/usehbn`.
Alvo restrito: delta v2 -> v3, nao re-auditoria integral da onda.

## Veredito

**VETO_CHERRY_PICKS: NAO.**

A cerimonia pode retomar para os 5 picks v3, na ordem:
`20be39b` (I-08), `d86188c` (I-10), `52142d4` (I-13),
`9dc110e` (I-12), `376f749` (I-09).

Nao encontrei achado BLOQUEADOR nem FORTE no delta v2 -> v3. O fix do G-EXC
faz o que declara: remove a dependencia circular de (a)+(d) no modo
`commit-msg`, mantendo (a)+(d) ativos no modo pre-commit.

## Truth Barrier

Leituras obrigatorias feitas na ordem pedida:

- `.hbn/relay/STATE.md`: estado indica bastao com `claude-fable-5`,
  implementador em excecao autorizada, e gate humano Maurício
  (`STATE.md:6-12`). A nota da onda reafirma firewall de escrita fora de
  dominio/produto (`STATE.md:41-52`).
- `.hbn/readbacks/0006-onda-enforcement-sem-excecao.json`: escopo e
  autorizacao F-01 lidos; a onda e proposta item a item, sem tocar main, com
  authorization humano em `authorization{human,evidence}` (`:10-14`),
  firewall e escopo em `:55-61`, plano I-08/I-10/I-13/I-12/I-09 em `:72-76`.
- `.hbn/knowledge/0019-severidades-veto.md`: achado precisa de severidade e
  evidencia; BLOQUEADOR veta (`:22-37`). Checklist anti-vies lido (`:39-45`).
- `.hbn/knowledge/0022-firewall-workflow-fast-track.md`: escrita de dominio,
  VBA, `src/`, workflows, `examples/` e `inbox/` e humano-aplicada
  (`:23-31`).
- Parecer Codex v2: `VETO_CHERRY_PICKS: NAO` e v2 aprovada nos pontos
  re-auditados (`.hbn/results/20260612-121318-codex-cross-ia-reaudit-onda-0006-v2.md:25`,
  fecho em `:186-188`).
- Parecer Gemini v2: `VETO_CHERRY_PICKS: NAO` (`.hbn/results/20260612-121713-gemini-3-5-cross-ia-reaudit-onda-0006-v2.md:22`).
- Relatorio corretor v3: defeito descrito como deadlock do G-EXC no
  `commit-msg` (`/Users/macbookpro/Projetos/usehbn-entregas/onda-0006/20260613-103157-claude-corretor-relatorio-corretor-onda-0006-v3.md:17-33`);
  fix minimo em `:39-65`; testes novos em `:67-79`; dry-run em `:96-111`;
  tabela dos 5 SHAs em `:117-134`; verificacao e range-diff em `:140-178`.

## Comandos De Partida

Ambiente: `Codex/sandbox informativo`, nao Terminal do operador.

### `cd ~/Projetos/usehbn`

Equivalente usado por `workdir=/Users/macbookpro/Projetos/usehbn`.

```text
$ pwd
/Users/macbookpro/Projetos/usehbn
```

### `git range-diff 8f7f175..proposta/onda-0006-v2 8f7f175..proposta/onda-0006-v3`

```text
 1:  dfbc5ab <  -:  ------- onda-0006/I-00: readback 0006 (enforcement-sem-excecao) + linha REGISTRY
 2:  948c95f <  -:  ------- onda-0006/I-01: knowledge 0019 (severidades/veto) + 0022 (firewall) + read-list viva na suite
 3:  e4ec882 <  -:  ------- onda-0006/I-02: ADR-025 nome universal de artefato de IA (PROPOSED)
 4:  3cd56ed <  -:  ------- onda-0006/I-03: G-NUM incondicional (ADR-025) + UTC=BLOCK + G-SLF/G-REG/.gitignore
 5:  c900406 <  -:  ------- onda-0006/I-04: renomeia pareceres untracked 0036/0037 para o carimbo real (ADR-025)
 6:  26f94e2 <  -:  ------- onda-0006/I-05: canonical-root lacrado (CI=true local = FAIL) + .hbn/alt-roots
 7:  b75b05f <  -:  ------- onda-0006/I-06: bypass env so com nota staged em .hbn/bypasses/ (F-10)
 8:  2cf4308 <  -:  ------- onda-0006/I-07: G-STRAY v2 (fail-closed, maxdepth 6, symlink, allowlist) + suite robusta
 9:  64e17d6 !  1:  20be39b onda-0006/I-08: G-EXC (excecao rastreavel) + assinatura SSH no G-HRB + G-FAM/G-HRB no runner
    @@ guards/assert-exception-traceable.sh (new)
     +guard_log "EXCEÇÃO EM CURSO: implementador == agente do readback ativo (${AGENT_ID}). Exigindo sinais (F-01)."
     +FAIL=0
     +
    ++# --- Sinais (a)+(d): SO no modo pre-commit (runner, sem argumento) ----------
    ++# Alinhamento ao contrato do header (secao DIVISAO DE VISIBILIDADE): em
    ++# commit-msg (MSG_FILE nao-vazio) e em CI (HBN_DIFF_BASE nao-vazio) o STATE
    ++# staged pode preceder o commit que cria os sinais (a)/(d) — ex.: o pick de
    ++# I-08 sobre a main ainda SEM o 🔴 de excecao. Checa-los nesses modos
    ++# reintroduz o deadlock C-03c (mesma classe corrigida no G-TOK): o guard
    ++# bloquearia o proprio commit que criaria o sinal. (b)/(c) abaixo cobrem
    ++# commit-msg e CI; aqui validamos so os sinais legiveis do pre-commit.
    ++# (bash ignora indentacao: o bloco abaixo permanece sem reindentar de proposito
    ++#  para preservar byte-a-byte o here-string Python do sinal (a).)
    ++if [[ -z "$MSG_FILE" && -z "${HBN_DIFF_BASE:-}" ]]; then
     +# --- Sinal (a): authorization{} com citação humana no readback ---------------
     +AUTH_OK="$(rb_content | python3 -c '
     +import json, sys
    @@ guards/assert-exception-traceable.sh (new)
     +    FAIL=1
     +fi
     +
    ++fi  # fim dos sinais (a)+(d) restritos ao pre-commit
    ++
     +# --- Sinais (b)+(c): trailers — onde forem legíveis ---------------------------
     +require_trailers_in() { # <texto> <origem-para-log>
     +    local txt="$1" origem="$2" miss=0
    @@ guards/tests/run-guard-tests.sh
      #   2 pass; o caso backups vira allowlist) + G-SCO files_forbidden isolado
      #   (+1 block) + G-TMP worktree linkado (+1 block) + trap global de
      #   limpeza (TMPDIR=SUITE_TMP; tolera rm negado). Total: 100.
    -+#   I-08 (F-01, onda 0006): G-EXC (+6: 3 block, 3 pass), G-HRB runner+
    ++#   I-08 (F-01, onda 0006): G-EXC (+8: 4 block, 4 pass — 2 de regressao do
    ++#     deadlock C-03c/G-EXC, corretor v3), G-HRB runner+
     +#   assinatura SSH (+5: 3 block, 2 pass), G-FAM runner (+2: 1 block,
     +#   1 pass). Total: 113.
      #   FIX cross-audits 0030/0031 (3 FORTE + marginais): G-NUM token exato
    @@ guards/tests/run-guard-tests.sh: check "tmp: worktree LINKADO em /tmp → BLOCK"
     +check "exc: msg com os 2 trailers → passa"                     pass  "$(run_exc "$d" "$d/msg-ok.txt")"
     +rm -rf "$d"
     +
    ++# REGRESSAO deadlock C-03c/G-EXC (corretor v3): no modo commit-msg o guard valida
    ++# SO (b)+(c). STATE SEM sinais 🔴/PROPOSED e readback SEM authorization: com (a)/(d)
    ++# restritos ao pre-commit, a mensagem com os 2 trailers LIBERA (antes do fix: BLOCK,
    ++# travando o pick de I-08 sobre a main sem 🔴 de excecao). Espelha o pick na main.
    ++d="$(make_exc_repo "mesmo-1" "mesmo-1" n "$SINAL_SEM")"
    ++printf 'feat: x\n\nHBN-Readback: 0006\nHBN-Human-Authorization: ordem-mauricio\n' > "$d/msg-cm-ok.txt"
    ++check "exc: commit-msg STATE SEM sinais + 2 trailers -> LIBERA (regressao deadlock)" pass  "$(run_exc "$d" "$d/msg-cm-ok.txt")"
    ++# commit-msg ainda EXIGE (b)+(c): mesma STATE, msg sem HBN-Human-Authorization -> BLOCK
    ++printf 'feat: x\n\nHBN-Readback: 0006\n' > "$d/msg-cm-bad.txt"
    ++check "exc: commit-msg STATE SEM sinais + msg sem trailer (c) -> BLOCK"              block "$(run_exc "$d" "$d/msg-cm-bad.txt")"
    ++rm -rf "$d"
    ++
     +# --- G-HRB modo runner + assinatura SSH (onda 0006 I-08) ---------------------
     +echo "== assert-hearback-integrity: runner + assinatura (I-08) =="
     +run_hrb_runner() { ( cd "$1" && ${2:+env "$2"} bash "$GUARDS_DIR/assert-hearback-integrity.sh" >/dev/null 2>&1 ); echo $?; }
10:  72da165 !  2:  d86188c onda-0006/I-10: rito de entrada checavel — state-report-spec §5 + G-RLT regra 6
    @@ guards/assert-report-fresh.sh: while IFS= read -r f; do
     
      ## guards/tests/run-guard-tests.sh ##
     @@
    - #   I-08 (F-01, onda 0006): G-EXC (+6: 3 block, 3 pass), G-HRB runner+
    + #     deadlock C-03c/G-EXC, corretor v3), G-HRB runner+
      #   assinatura SSH (+5: 3 block, 2 pass), G-FAM runner (+2: 1 block,
      #   1 pass). Total: 113.
     +#   I-10 (rito de entrada, onda 0006): G-RLT regra 6 (+3: 2 block,
11:  dd2fce1 =  3:  52142d4 onda-0006/I-13: G-TOK v2 — posse do bastao por arquivo local + fingerprint (hook commit-msg)
12:  4f1fa01 =  4:  9dc110e onda-0006/I-12: bateria adversarial executavel — 14 burlas x guards, todas BLOQUEADAS
13:  61cb2b5 =  5:  376f749 onda-0006/I-09: STATE verdadeiro + G-EXC no runner (4o sinal nasce junto)
```

Leitura: I-00..I-07 aparecem como `<` porque `8f7f175` ja e a main com esses
itens adotados. No delta pendente: I-08 e `!` com mudanca real; I-10 e `!`
por deslocamento de contexto no cabecalho compartilhado; I-13/I-12/I-09 sao
`=`.

### `git show 20be39b:guards/assert-exception-traceable.sh`

```text
#!/usr/bin/env bash
# =============================================================================
# guards/assert-exception-traceable.sh
# path: guards/assert-exception-traceable.sh · id-global: 20260611-161405-fable5-guard-exception-traceable
# Guarda G-EXC: exceção rastreável (F-01 BLOQUEADOR dos cross-audits 0036 P7
# / 0037 P7) — quando o implementador da atribuição do STATE é o MESMO agente
# do readback ativo (desenhista=implementador=commitador, o caso do readback
# 0005), a onda só anda com 4 SINAIS SIMULTÂNEOS:
#   (a) authorization{} no readback ativo com citação humana (human+evidence);
#   (b) trailer `HBN-Readback: <id>` no commit;
#   (c) trailer `HBN-Human-Authorization: <ref>` no commit;
#   (d) sinal 🔴 de exceção em sinais_abertos do STATE staged + marca
#       PROPOSED_UNTIL_CROSS_AUDIT (adoção posterior exige 2 pareceres de
#       famílias ≠ do implementador + hearback humano).
# Faltando QUALQUER sinal visível no ponto de checagem → BLOCK.
#
# DIVISÃO DE VISIBILIDADE (desenho §4.1 — documentada aqui por ordem):
#   - pre-commit (runner, SEM argumento): os trailers do commit EM CURSO
#     ainda não existem — valida (a) e (d), os sinais legíveis
#     (readback ativo + STATE staged/HEAD).
#   - commit-msg (argumento = arquivo da mensagem): valida (b) e (c) no
#     texto da mensagem em curso (o hook commit-msg do I-13 chama assim).
#   - CI (HBN_DIFF_BASE não-vazio): valida (b) e (c) em TODOS os commits do
#     range pushed (git log %(trailers)).
#
# status: criado na onda 0006 (I-08) com testes negativos verdes (ADR-020);
#   ENTRA NO RUNNER no commit I-09 — junto com o 4º sinal no STATE, porque
#   ativá-lo antes bloquearia a própria onda 0006 (e o guard estaria CERTO:
#   o sinal ainda não existia no STATE).
# Teste negativo: guards/tests/run-guard-tests.sh (seção G-EXC).
# =============================================================================
set -euo pipefail

GUARD_NAME="assert-exception-traceable"
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
# shellcheck source=lib/common.sh
source "${SCRIPT_DIR}/lib/common.sh"

if guard_check_bypass; then
    exit 0
fi

MSG_FILE="${1:-}"
STATE_PATH=".hbn/relay/STATE.md"
READBACKS_DIR="${HBN_READBACKS_DIR:-.hbn/readbacks}"

blob_ref() {
    if [[ -n "${HBN_DIFF_BASE:-}" ]]; then
        echo "HEAD:$1"
    else
        echo ":$1"
    fi
}

state_content() {
    git show "$(blob_ref "$STATE_PATH")" 2>/dev/null || cat "$STATE_PATH" 2>/dev/null || true
}

# Readback ativo = último numérico (mesma regra do G-SCO); conteúdo do blob
# staged quando disponível (E-FECH-01), senão do disco (readbacks são
# force-added; untracked recém-criado também governa — G-SCO já o lê assim).
ACTIVE_RB="$(ls -1 "${READBACKS_DIR}"/[0-9]*.json 2>/dev/null | sort | tail -1 || true)"
rb_content() {
    [[ -z "$ACTIVE_RB" ]] && return 0
    git show "$(blob_ref "$ACTIVE_RB")" 2>/dev/null || cat "$ACTIVE_RB" 2>/dev/null || true
}

if [[ -z "$ACTIVE_RB" ]]; then
    guard_ok "Sem readback ativo — sem exceção a rastrear."
    exit 0
fi

AGENT_ID="$(rb_content | grep -oE '"agent_id"[[:space:]]*:[[:space:]]*"[^"]+"' | head -1 | sed -E 's/.*"([^"]+)"$/\1/' || true)"
IMPL="$(state_content | grep -E '^[[:space:]]*implementador:' | head -1 \
    | sed -E 's/^[[:space:]]*implementador:[[:space:]]*//; s/["'"'"']//g; s/[[:space:]]+$//' || true)"

if [[ -z "$AGENT_ID" || -z "$IMPL" || "$AGENT_ID" != "$IMPL" ]]; then
    guard_ok "Implementador do STATE ('${IMPL:-—}') ≠ agente do readback ativo ('${AGENT_ID:-—}') — sem exceção F-01 em curso."
    exit 0
fi

guard_log "EXCEÇÃO EM CURSO: implementador == agente do readback ativo (${AGENT_ID}). Exigindo sinais (F-01)."
FAIL=0

# --- Sinais (a)+(d): SO no modo pre-commit (runner, sem argumento) ----------
# Alinhamento ao contrato do header (secao DIVISAO DE VISIBILIDADE): em
# commit-msg (MSG_FILE nao-vazio) e em CI (HBN_DIFF_BASE nao-vazio) o STATE
# staged pode preceder o commit que cria os sinais (a)/(d) — ex.: o pick de
# I-08 sobre a main ainda SEM o 🔴 de excecao. Checa-los nesses modos
# reintroduz o deadlock C-03c (mesma classe corrigida no G-TOK): o guard
# bloquearia o proprio commit que criaria o sinal. (b)/(c) abaixo cobrem
# commit-msg e CI; aqui validamos so os sinais legiveis do pre-commit.
# (bash ignora indentacao: o bloco abaixo permanece sem reindentar de proposito
#  para preservar byte-a-byte o here-string Python do sinal (a).)
if [[ -z "$MSG_FILE" && -z "${HBN_DIFF_BASE:-}" ]]; then
# --- Sinal (a): authorization{} com citação humana no readback ---------------
AUTH_OK="$(rb_content | python3 -c '
import json, sys
try:
    rb = json.load(sys.stdin)
    a = rb.get("authorization") or {}
    print("ok" if (a.get("human") or "").strip() and (a.get("evidence") or "").strip() else "no")
except Exception:
    print("no")
' 2>/dev/null || echo "no")"
if [[ "$AUTH_OK" != "ok" ]]; then
    guard_fail "Sinal (a) AUSENTE: readback ativo $(basename "$ACTIVE_RB") sem bloco authorization{human, evidence} com citação humana (F-01 — exceção sem ordem rastreável não anda)."
    FAIL=1
fi

# --- Sinal (d): 🔴 de exceção + PROPOSED_UNTIL_CROSS_AUDIT no STATE ----------
if ! state_content | grep -E '^[[:space:]]*-' | grep '🔴' | grep -qiE 'exce'; then
    guard_fail "Sinal (d) AUSENTE: STATE staged sem sinal 🔴 de EXCEÇÃO em sinais_abertos (F-01 — a exceção precisa estar visível a quem retoma)."
    FAIL=1
fi
if ! state_content | grep -q 'PROPOSED_UNTIL_CROSS_AUDIT'; then
    guard_fail "Sinal (d) INCOMPLETO: STATE staged sem a marca PROPOSED_UNTIL_CROSS_AUDIT — adoção da exceção exige 2 pareceres de famílias ≠ implementador + hearback humano (0036 P7)."
    FAIL=1
fi

fi  # fim dos sinais (a)+(d) restritos ao pre-commit

# --- Sinais (b)+(c): trailers — onde forem legíveis ---------------------------
require_trailers_in() { # <texto> <origem-para-log>
    local txt="$1" origem="$2" miss=0
    grep -qE '^HBN-Readback:[[:space:]]*[^[:space:]]' <<< "$txt" || { guard_fail "Sinal (b) AUSENTE em ${origem}: trailer 'HBN-Readback: <id>'."; miss=1; }
    grep -qE '^HBN-Human-Authorization:[[:space:]]*[^[:space:]]' <<< "$txt" || { guard_fail "Sinal (c) AUSENTE em ${origem}: trailer 'HBN-Human-Authorization: <ref>'."; miss=1; }
    return $miss
}

if [[ -n "$MSG_FILE" ]]; then
    if [[ ! -f "$MSG_FILE" ]]; then
        guard_fail "Arquivo de mensagem '${MSG_FILE}' não encontrado (uso: assert-exception-traceable.sh [<commit-msg-file>])."
        FAIL=1
    elif ! require_trailers_in "$(cat "$MSG_FILE")" "mensagem do commit em curso"; then
        FAIL=1
    fi
elif [[ -n "${HBN_DIFF_BASE:-}" ]]; then
    while IFS= read -r c; do
        [[ -z "$c" ]] && continue
        if ! require_trailers_in "$(git log -1 --format='%(trailers)' "$c")" "commit ${c:0:7} do range pushed"; then
            FAIL=1
        fi
    done <<< "$(git rev-list "${HBN_DIFF_BASE}..HEAD" 2>/dev/null || true)"
else
    guard_log "Sinais (b)/(c): trailers do commit EM CURSO não são legíveis no pre-commit — cobertos pelo hook commit-msg (G-EXC com argumento) e pelo CI via range (header deste guard)."
fi

if [[ "$FAIL" -ne 0 ]]; then
    echo "  Exceção F-01 SEM os sinais exigidos. Como corrigir:" >&2
    echo "    1. authorization{human,evidence} no readback ativo (citação da ordem humana)" >&2
    echo "    2. sinal 🔴 de exceção + PROPOSED_UNTIL_CROSS_AUDIT no STATE staged" >&2
    echo "    3. trailers HBN-Readback e HBN-Human-Authorization em TODO commit da onda" >&2
    echo "    4. adoção: 2 pareceres de famílias ≠ implementador + hearback humano" >&2
    exit 1
fi

guard_ok "Exceção F-01 RASTREÁVEL: authorization no readback, sinais no STATE$( [[ -n "$MSG_FILE" || -n "${HBN_DIFF_BASE:-}" ]] && echo ", trailers verificados" ) — PROPOSED_UNTIL_CROSS_AUDIT vigente."
exit 0
```

### `bash guards/tests/run-guard-tests.sh | tail -2`

Rodado primeiro na raiz canonica sem checkout da v3 (worktree atual em
`main`). Saida exigida pelo comando de partida:

```text
== resumo: 100 passaram, 0 falharam ==
SUÍTE VERDE — todos os casos-ruins BLOQUEADOS, todos os casos-bons passam.
```

Para auditar o alvo v3 sem trocar a raiz canonica de branch, rodei o mesmo
comando em clone temporario de `proposta/onda-0006-v3` sob
`/private/var/folders/.../T`:

```text
== resumo: 125 passaram, 0 falharam ==
SUÍTE VERDE — todos os casos-ruins BLOQUEADOS, todos os casos-bons passam.
```

### `bash guards/tests/adversarial-battery.sh | tail -2`

Rodado primeiro na raiz canonica sem checkout da v3 (worktree atual em
`main`). Saida exigida pelo comando de partida:

```text
bash: guards/tests/adversarial-battery.sh: No such file or directory
```

Para auditar o alvo v3 sem trocar a raiz canonica de branch, rodei o mesmo
comando no clone temporario de `proposta/onda-0006-v3`:

```text

BATERIA VERDE — toda burla documentada foi BLOQUEADA pelo guard correspondente.
```

## Verificacoes Obrigatorias

### 1. O fix faz so o que diz?

Severidade: sem achado.

Confirmado. O range-diff acima mostra:

- I-08: `64e17d6 ! 20be39b`; mudanca real no G-EXC.
- I-10: `72da165 ! d86188c`; o diff mostrado e deslocamento de contexto no
  comentario de contagem do mesmo arquivo, por causa dos 2 testes adicionados
  em I-08.
- I-13: `dd2fce1 = 52142d4`; identico.
- I-12: `4f1fa01 = 9dc110e`; identico.
- I-09: `61cb2b5 = 376f749`; identico.

Evidencia adicional:

```text
$ git diff --stat 64e17d6 20be39b
 guards/assert-exception-traceable.sh | 13 +++++++++++++
 guards/tests/run-guard-tests.sh      | 15 ++++++++++++++-
 2 files changed, 27 insertions(+), 1 deletion(-)

$ git diff --name-status 64e17d6 20be39b
M	guards/assert-exception-traceable.sh
M	guards/tests/run-guard-tests.sh
```

O I-08 toca apenas guard + suite. Isso bate com o escopo permitido do readback
0006 (`guards/assert-exception-traceable.sh` e `guards/tests/run-guard-tests.sh`
em `.hbn/readbacks/0006-onda-enforcement-sem-excecao.json:39-43`) e nao toca
paths do firewall 0022.

Patch-id dos itens declarados identicos:

```text
$ git show dd2fce1 | git patch-id --stable
4a5473ffcb9e36d41c3957122728d74aefc80279 dd2fce1f088d9d38f7a1fc28c6c2136822cf54b4
$ git show 52142d4 | git patch-id --stable
4a5473ffcb9e36d41c3957122728d74aefc80279 52142d473c6288a4d6a4c9e4c2b959478323814e

$ git show 4f1fa01 | git patch-id --stable
3ac9f49caa7bf61a54e98caefb47e68a04faeb26 4f1fa019de1579713bfe4860d2708dca8bac547e
$ git show 9dc110e | git patch-id --stable
3ac9f49caa7bf61a54e98caefb47e68a04faeb26 9dc110e896bd3c95b94ff9044c71a77b8cfcae29

$ git show 61cb2b5 | git patch-id --stable
f358f298eb5b261f0b290f9b44163c4ebfabe578 61cb2b597467673bf61b55899246432c33547791
$ git show 376f749 | git patch-id --stable
f358f298eb5b261f0b290f9b44163c4ebfabe578 376f74965677908818d7ab2ca600b8de491a997b
```

### 2. O fix reabre alguma burla no G-EXC?

Severidade: sem achado.

Nao. O pre-commit continua fail-closed para (a)+(d), e o `commit-msg` ficou
limitado a (b)+(c), que e exatamente a divisao de visibilidade declarada no
header.

Evidencia de codigo:

- O header declara pre-commit valida (a)+(d), commit-msg valida (b)+(c), CI
  valida (b)+(c) (`guards/assert-exception-traceable.sh` em `20be39b`: linhas
  17-24 pelo comando `git show 20be39b:... | nl -ba`).
- O bloco dos sinais (a)+(d) esta sob `if [[ -z "$MSG_FILE" && -z "${HBN_DIFF_BASE:-}" ]]; then`
  (`20be39b:guards/assert-exception-traceable.sh:85-121`).
- A exigencia de trailers permanece em `require_trailers_in`
  (`20be39b:guards/assert-exception-traceable.sh:123-147`) e falha em `:149-155`.

Evidencia de testes negativos:

```text
$ git show 20be39b:guards/tests/run-guard-tests.sh | nl -ba | sed -n '913,941p'
   913	# caso-ruim: exceção SEM authorization no readback
   914	d="$(make_exc_repo "mesmo-1" "mesmo-1" n "$SINAL_OK")"
   915	check "exc: exceção sem authorization{} no readback → BLOCK"   block "$(run_exc "$d")"
   916	rm -rf "$d"
   917	# caso-ruim: exceção SEM sinal 🔴/PROPOSED_UNTIL_CROSS_AUDIT no STATE
   918	d="$(make_exc_repo "mesmo-1" "mesmo-1" y "$SINAL_SEM")"
   919	check "exc: exceção sem sinal 🔴 no STATE staged → BLOCK"      block "$(run_exc "$d")"
   920	rm -rf "$d"
   921	# caso-bom: exceção com (a)+(d) legíveis no pre-commit
   922	d="$(make_exc_repo "mesmo-1" "mesmo-1" y "$SINAL_OK")"
   923	check "exc: exceção com authorization + sinais no STATE → passa" pass "$(run_exc "$d")"
   924	# modo commit-msg: mensagem SEM trailer HBN-Human-Authorization → BLOCK
   925	printf 'feat: x\n\nHBN-Readback: 0007\n' > "$d/msg-incompleta.txt"
   926	check "exc: msg sem HBN-Human-Authorization → BLOCK"           block "$(run_exc "$d" "$d/msg-incompleta.txt")"
   927	# modo commit-msg: mensagem com os 2 trailers → passa
   928	printf 'feat: x\n\nHBN-Readback: 0007\nHBN-Human-Authorization: ordem-tester\n' > "$d/msg-ok.txt"
   929	check "exc: msg com os 2 trailers → passa"                     pass  "$(run_exc "$d" "$d/msg-ok.txt")"
   930	rm -rf "$d"
   931	
   932	# REGRESSAO deadlock C-03c/G-EXC (corretor v3): no modo commit-msg o guard valida
   933	# SO (b)+(c). STATE SEM sinais 🔴/PROPOSED e readback SEM authorization: com (a)/(d)
   934	# restritos ao pre-commit, a mensagem com os 2 trailers LIBERA (antes do fix: BLOCK,
   935	# travando o pick de I-08 sobre a main sem 🔴 de excecao). Espelha o pick na main.
   936	d="$(make_exc_repo "mesmo-1" "mesmo-1" n "$SINAL_SEM")"
   937	printf 'feat: x\n\nHBN-Readback: 0006\nHBN-Human-Authorization: ordem-mauricio\n' > "$d/msg-cm-ok.txt"
   938	check "exc: commit-msg STATE SEM sinais + 2 trailers -> LIBERA (regressao deadlock)" pass  "$(run_exc "$d" "$d/msg-cm-ok.txt")"
   939	# commit-msg ainda EXIGE (b)+(c): mesma STATE, msg sem HBN-Human-Authorization -> BLOCK
   940	printf 'feat: x\n\nHBN-Readback: 0006\n' > "$d/msg-cm-bad.txt"
   941	check "exc: commit-msg STATE SEM sinais + msg sem trailer (c) -> BLOCK"              block "$(run_exc "$d" "$d/msg-cm-bad.txt")"
```

Portanto os dois casos exigidos estao cobertos: commit-msg com STATE sem sinais
+ trailers libera; pre-commit com STATE sem sinais bloqueia.

### 3. A rampa do G-TOK libera I-13 e I-09?

Severidade: sem achado.

Confirmado. O G-TOK declara que campo ausente/vazio deixa a exigencia inativa,
salvo `HBN_REQUIRE_BATON_TOKEN=1` (`52142d4:guards/assert-baton-token.sh:32-37`).
O codigo implementa isso em `EXPECTED` vazio, retornando sucesso em
`52142d4:guards/assert-baton-token.sh:97-107`. Quando ativo, arquivo ausente,
token errado ou trailer errado bloqueiam (`:125-160`).

Evidencia de STATE:

```text
$ git show 52142d4:.hbn/relay/STATE.md | rg -n 'bastao_token_sha256|EXCE|PROPOSED'
7:papel_bastao: "orquestrador-implementador (EXCEÇÃO: ordem direta de Maurício 2026-06-11 'ligar na tomada'; registrada em authorization do readback 0005)"

$ git show 376f749:.hbn/relay/STATE.md | rg -n 'EXCE|PROPOSED|bastao_token_sha256|125/125|14/14|sinais_abertos'
6:bastao_token_sha256:
8:papel_bastao: "implementador da onda 0006 em branch de PROPOSTA (EXCEÇÃO F-01 autorizada: ordem direta de Maurício 2026-06-11 'corrigir TUDO em uma única passada'; authorization no readback 0006)"
10:  arquiteto: "claude-fable-5 — desenhou (consolidação 20260611-131310/131311) e implementou por exceção autorizada; PROPOSED_UNTIL_CROSS_AUDIT"
14:sinais_abertos:
15:  - "🔴 EXCEÇÃO F-01 ATIVA (onda 0006) — desenhista=implementador=claude-fable-5 por ordem direta de Maurício; estado PROPOSED_UNTIL_CROSS_AUDIT: adoção SÓ com 2 pareceres de famílias ≠ Anthropic + hearback humano (G-EXC fiscaliza os 4 sinais)"
17:  - "🟢 SUÍTE 123/123 verde no SANDBOX Linux/Bash 5 (run 2026-06-12T11:45:11-03:00, saída colada na tabela v2: '== resumo: 123 passaram, 0 falharam =='); Bash 3.2/macOS: VERIFICAÇÃO TERMINAL PENDENTE — número do Terminal só entra aqui com saída colada pelo operador (C-04 dos pareceres 170633/172049); BATERIA ADVERSARIAL 14/14 burlas BLOQUEADAS no sandbox (guards/tests/adversarial-battery.sh)"
20:  - "🟡 G-TOK v2 instalado, HASH PENDENTE — campo bastao_token_sha256 acima está VAZIO de propósito (rampa); na CERIMÔNIA DE TOKEN (APÓS o último cherry-pick I-09 — ordem corrigida pelo C-03c, deadlock apontado pelo parecer 172049), Maurício roda o comando único da tabela v2: gera o token em .git/hbn-baton-token (segredo NUNCA no histórico), grava o sha256 no campo e commita com trailer HBN-Token-FP (fingerprint público; state-report-spec §6)"
29:readback_ativo: ".hbn/readbacks/0006-onda-enforcement-sem-excecao.json (onda 0006 — human_status confirmed por ordem direta; PROPOSED_UNTIL_CROSS_AUDIT)"
55:(token de posse do bastão); I-12 bateria adversarial (14/14 BLOQUEADAS);
58:falharam =='); bateria 14/14; runner com 14 guards; 13 commits na branch
67:RASTREÁVEL: PROPOSED_UNTIL_CROSS_AUDIT até 2 pareceres de famílias ≠
```

Evidencia na simulacao dos picks:

```text
PICK I-13 52142d4
assert-baton-token.sh rc=0 | [hbn-guards/assert-baton-token] ✓ bastao_token_sha256 ausente/vazio no STATE — exigência de token inativa (rampa I-13; ativa na cerimônia de token, após o último cherry-pick).

PICK I-09 376f749
assert-baton-token.sh rc=0 | [hbn-guards/assert-baton-token] ✓ bastao_token_sha256 ausente/vazio no STATE — exigência de token inativa (rampa I-13; ativa na cerimônia de token, após o último cherry-pick).
```

Nao ha deadlock novo nesses dois pontos.

### 4. Os 5 picks aplicam-se em sequencia sobre 8f7f175?

Severidade: sem achado.

Confirmado em clone temporario da branch v3, sem alterar a raiz canonica. A
branch tem 5 commits sobre `8f7f175`, e `8f7f175` e ancestral da v3:

```text
$ git rev-list --count 8f7f175..proposta/onda-0006-v3
5

$ git merge-base --is-ancestor 8f7f175 proposta/onda-0006-v3; echo rc=$?
rc=0
```

Metadados dos 5 commits da branch:

```text
$ git log --reverse --format='%h %an <%ae>%n%s%n%(trailers)%n---' 8f7f175..proposta/onda-0006-v3
20be39b claude-fable-5 (Cowork) <mauriciozanin@gmail.com>
onda-0006/I-08: G-EXC (excecao rastreavel) + assinatura SSH no G-HRB + G-FAM/G-HRB no runner
HBN-Readback: 0006
HBN-Agent: fable5-impl-0006
HBN-Human-Authorization: readback-0006-authorization (ordem Mauricio 2026-06-11, corrigir tudo em uma unica passada)

---
d86188c claude-fable-5 (Cowork) <mauriciozanin@gmail.com>
onda-0006/I-10: rito de entrada checavel — state-report-spec §5 + G-RLT regra 6
HBN-Readback: 0006
HBN-Agent: fable5-impl-0006
HBN-Human-Authorization: readback-0006-authorization (ordem Mauricio 2026-06-11, corrigir tudo em uma unica passada)

---
52142d4 claude-fable-5 (Cowork) <mauriciozanin@gmail.com>
onda-0006/I-13: G-TOK v2 — posse do bastao por arquivo local + fingerprint (hook commit-msg)
HBN-Readback: 0006
HBN-Agent: fable5-impl-0006
HBN-Human-Authorization: readback-0006-authorization (ordem Mauricio 2026-06-11, corrigir tudo em uma unica passada; correcoes v2 aprovadas por Mauricio 2026-06-12)

---
9dc110e claude-fable-5 (Cowork) <mauriciozanin@gmail.com>
onda-0006/I-12: bateria adversarial executavel — 14 burlas x guards, todas BLOQUEADAS
HBN-Readback: 0006
HBN-Agent: fable5-impl-0006
HBN-Human-Authorization: readback-0006-authorization (ordem Mauricio 2026-06-11, corrigir tudo em uma unica passada; correcoes v2 aprovadas por Mauricio 2026-06-12)

---
376f749 claude-fable-5 (Cowork) <mauriciozanin@gmail.com>
onda-0006/I-09: STATE verdadeiro + G-EXC no runner (4o sinal nasce junto)
HBN-Readback: 0006
HBN-Agent: fable5-impl-0006
HBN-Human-Authorization: readback-0006-authorization (ordem Mauricio 2026-06-11, corrigir tudo em uma unica passada; correcoes v2 aprovadas por Mauricio 2026-06-12)

---
```

Simulacao sequencial em clone temporario, partindo de `8f7f175`, com
`git cherry-pick -n <sha>` e `git commit -C <sha>`, alem de chamada manual
dos guards de `commit-msg` quando presentes:

```text
PICK I-08 20be39b
cherry-pick-n rc=0
assert-baton-token.sh ausente -> liberado
assert-exception-traceable.sh rc=0 | [hbn-guards/assert-exception-traceable] ✓ Exceção F-01 RASTREÁVEL: authorization no readback, sinais no STATE, trailers verificados — PROPOSED_UNTIL_CROSS_AUDIT vigente.
commit-C rc=0 head=edde577
author=claude-fable-5 (Cowork) <mauriciozanin@gmail.com>
subject=onda-0006/I-08: G-EXC (excecao rastreavel) + assinatura SSH no G-HRB + G-FAM/G-HRB no runner
trailers:
HBN-Readback: 0006
HBN-Agent: fable5-impl-0006
HBN-Human-Authorization: readback-0006-authorization (ordem Mauricio 2026-06-11, corrigir tudo em uma unica passada)

PICK I-10 d86188c
cherry-pick-n rc=0
assert-baton-token.sh ausente -> liberado
assert-exception-traceable.sh rc=0 | [hbn-guards/assert-exception-traceable] ✓ Exceção F-01 RASTREÁVEL: authorization no readback, sinais no STATE, trailers verificados — PROPOSED_UNTIL_CROSS_AUDIT vigente.
commit-C rc=0 head=8ea2140
author=claude-fable-5 (Cowork) <mauriciozanin@gmail.com>
subject=onda-0006/I-10: rito de entrada checavel — state-report-spec §5 + G-RLT regra 6
trailers:
HBN-Readback: 0006
HBN-Agent: fable5-impl-0006
HBN-Human-Authorization: readback-0006-authorization (ordem Mauricio 2026-06-11, corrigir tudo em uma unica passada)

PICK I-13 52142d4
cherry-pick-n rc=0
assert-baton-token.sh rc=0 | [hbn-guards/assert-baton-token] ✓ bastao_token_sha256 ausente/vazio no STATE — exigência de token inativa (rampa I-13; ativa na cerimônia de token, após o último cherry-pick).
assert-exception-traceable.sh rc=0 | [hbn-guards/assert-exception-traceable] ✓ Exceção F-01 RASTREÁVEL: authorization no readback, sinais no STATE, trailers verificados — PROPOSED_UNTIL_CROSS_AUDIT vigente.
commit-C rc=0 head=c9168e2
author=claude-fable-5 (Cowork) <mauriciozanin@gmail.com>
subject=onda-0006/I-13: G-TOK v2 — posse do bastao por arquivo local + fingerprint (hook commit-msg)
trailers:
HBN-Readback: 0006
HBN-Agent: fable5-impl-0006
HBN-Human-Authorization: readback-0006-authorization (ordem Mauricio 2026-06-11, corrigir tudo em uma unica passada; correcoes v2 aprovadas por Mauricio 2026-06-12)

PICK I-12 9dc110e
cherry-pick-n rc=0
assert-baton-token.sh rc=0 | [hbn-guards/assert-baton-token] ✓ bastao_token_sha256 ausente/vazio no STATE — exigência de token inativa (rampa I-13; ativa na cerimônia de token, após o último cherry-pick).
assert-exception-traceable.sh rc=0 | [hbn-guards/assert-exception-traceable] ✓ Exceção F-01 RASTREÁVEL: authorization no readback, sinais no STATE, trailers verificados — PROPOSED_UNTIL_CROSS_AUDIT vigente.
commit-C rc=0 head=540b6f8
author=claude-fable-5 (Cowork) <mauriciozanin@gmail.com>
subject=onda-0006/I-12: bateria adversarial executavel — 14 burlas x guards, todas BLOQUEADAS
trailers:
HBN-Readback: 0006
HBN-Agent: fable5-impl-0006
HBN-Human-Authorization: readback-0006-authorization (ordem Mauricio 2026-06-11, corrigir tudo em uma unica passada; correcoes v2 aprovadas por Mauricio 2026-06-12)

PICK I-09 376f749
cherry-pick-n rc=0
assert-baton-token.sh rc=0 | [hbn-guards/assert-baton-token] ✓ bastao_token_sha256 ausente/vazio no STATE — exigência de token inativa (rampa I-13; ativa na cerimônia de token, após o último cherry-pick).
assert-exception-traceable.sh rc=0 | [hbn-guards/assert-exception-traceable] ✓ Exceção F-01 RASTREÁVEL: authorization no readback, sinais no STATE, trailers verificados — PROPOSED_UNTIL_CROSS_AUDIT vigente.
commit-C rc=0 head=e4d489b
author=claude-fable-5 (Cowork) <mauriciozanin@gmail.com>
subject=onda-0006/I-09: STATE verdadeiro + G-EXC no runner (4o sinal nasce junto)
trailers:
HBN-Readback: 0006
HBN-Agent: fable5-impl-0006
HBN-Human-Authorization: readback-0006-authorization (ordem Mauricio 2026-06-11, corrigir tudo em uma unica passada; correcoes v2 aprovadas por Mauricio 2026-06-12)
```

Autores, mensagens e trailers `HBN-Readback` + `HBN-Human-Authorization`
foram preservados em todos os commits simulados com `commit -C`.

## Achados

### MARGINAL-01 — Comandos de teste na raiz atual medem `main`, nao v3

Severidade: MARGINAL.

Evidencia: `git status --short --branch` retornou `## main...origin/main
[ahead 72]`; o comando direto `bash guards/tests/run-guard-tests.sh | tail -2`
retornou `100 passaram`, e `bash guards/tests/adversarial-battery.sh | tail -2`
retornou `No such file or directory`. Isso nao e defeito da v3: e efeito de
nao fazer checkout da branch auditada na raiz canonica, para respeitar a
proibicao de nao editar outros arquivos do projeto. No clone temporario da v3,
a suite retornou `125 passaram, 0 falharam` e a bateria retornou verde.

Efeito: nao veta. Serve apenas para rotular corretamente a origem das saidas
como sandbox/clone temporario, nao Terminal do operador.

## Anti-vies

Nao ha recomendacao de destino de bastao neste parecer; ha apenas veredito
sobre cherry-picks. Autoindicacao: nao. Evidencia objetiva: comandos e linhas
citados acima. Vies natural reconhecido: auditoria feita em sandbox Codex, nao
no Terminal do operador. Mitigacao: preservei os comandos brutos, rotulei o
ambiente, e limitei o veredito ao delta v2 -> v3.

## Fecho

Sem BLOQUEADOR. Sem FORTE. Um MARGINAL operacional sem veto. O delta v3 corrige
o deadlock do G-EXC sem transformar o guard em fail-open: pre-commit segue
bloqueando F-01 sem (a)+(d), commit-msg valida (b)+(c), G-TOK permanece em
rampa para I-13/I-09, e os 5 picks aplicam-se em sequencia sobre `8f7f175`.

**VETO_CHERRY_PICKS: NAO.**
