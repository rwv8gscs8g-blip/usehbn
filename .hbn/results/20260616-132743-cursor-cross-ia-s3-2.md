---
titulo: "Cross-audit independente — S3.2 (porta da frente + G-FRONTDOOR)"
tipo: audit-result
status: proposto
temperatura: frio
path: .hbn/results/20260616-132743-cursor-cross-ia-s3-2.md
id-global: 20260616-132743-cursor-cross-ia-s3-2
autor: cursor (auditor cruzado, família distinta de Codex e Gemini)
readback: 0031-porta-da-frente-cartoes
branch_auditada: proposta/reestruturacao-m-a-s0
base: 534907c
head: 4857081
created_at: "2026-06-16T13:27:43-03:00"
---

APROVA_0031: SIM

# Cross-audit S3.2 — Cursor

Auditoria independente. Não consultei parecer Gemini (inexistente no depósito à data desta auditoria). Só leitura; sem commit; `main` intocada (`4db6928`).

## Parte A — pontos de ataque (G-FRONTDOOR / S3.2)

### A1. Correção G-FRONTDOOR (positivo + negativo)

**PASSA** nos quatro lados cobertos pela suíte.

| caso | rc | esperado | evidência |
|---|---|---|---|
| `core/role-cards.md` válido staged | 0 | pass | `run-guard-tests.sh:1605-1607` |
| arquivo ausente no índice | 1 | block | `run-guard-tests.sh:1609-1612` |
| >140 linhas físicas | 1 | block | `run-guard-tests.sh:1614-1622` (+130 linhas extras) |
| read-list com 7 itens na PARTE A | 1 | block | `run-guard-tests.sh:1624-1632` |

Burla permanente B25 (`adversarial-battery.sh:390-414`) combina read-list estourada **e** inflação por linhas — bloqueada.

Reprodução local no repo atual: `bash guards/assert-frontdoor.sh` → `rc=0`, mensagem `32 linhas; read-list com 6 item(ns)`.

### A2. Fail-closed (ausente/ilegível)

**PASSA.**

```37:45:guards/assert-frontdoor.sh
if ! git cat-file -e "$ROLE_REF" 2>/dev/null; then
    guard_fail "Porta da frente ausente no indice/HEAD: ${ROLE_CARDS_PATH}."
    exit 1
fi
// ...
if [[ -z "$CONTENT" ]]; then
    guard_fail "Porta da frente ilegivel no indice/HEAD: ${ROLE_CARDS_PATH}."
```

Secção PARTE A ausente → `READLIST_ITEMS=MISSING` → block (`assert-frontdoor.sh:67-69`). Working tree solta não conta: blob staged local ou `HEAD` em CI (`assert-frontdoor.sh:6-7`, `README.md:92-93`).

### A3. Idempotência (P-CAND-02)

**PASSA** (ambiente sem lock de git, permissões completas).

| execução | `assert-frontdoor.sh` | `run-guard-tests.sh` |
|---|---|---|
| 1 | rc=0 | 160/160 verde |
| 2 | rc=0 | — |
| 3 | rc=0 | — |

`hbn-guards-runner.sh` → `rc=0` após as três execuções do guard isolado.

Nota ambiental: na sandbox restrita, `run-guard-tests.sh` e `adversarial-battery.sh` falharam com `fatal: not a git repository` ao criar repos temporários (mesmo padrão documentado no cross-audit S3.1). Com permissões normais, 160/160 e B1–B25 verdes.

### A4. Escopo `534907c..HEAD`

**PASSA.** Exatamente 10 arquivos, todos em `files_allowed` do readback 0031:

```
.hbn/messages/20260616-160400-codex-handoff-s3-2.md
.hbn/readbacks/0031-porta-da-frente-cartoes.json
.hbn/relay/STATE.md
REGISTRY.md
core/role-cards.md
guards/README.md
guards/assert-frontdoor.sh
guards/hbn-guards-runner.sh
guards/tests/adversarial-battery.sh
guards/tests/run-guard-tests.sh
```

Nenhum arquivo em `files_forbidden` tocado. Specs completas (`core/orchestrator-profile-spec.md`, `core/roles-assignment-spec.md`) permanecem intocadas.

### A5. Sem regressão

**PASSA.**

- `bash guards/tests/run-guard-tests.sh` → **160 passaram, 0 falharam** (permissões completas).
- `bash guards/tests/adversarial-battery.sh` → **B1–B25 BLOQUEADAS**, incluindo B25/G-FRONT.
- `bash guards/hbn-guards-runner.sh` → verde.
- `main` permanece em `4db6928`; branch `proposta/reestruturacao-m-a-s0` @ `4857081` (86 commits à frente de `main`).

### A6. Trailers contíguos (5 commits C1–C5)

**PASSA.** Commits `5eef11c` … `4857081`: cada um com os três trailers contíguos, sem linha em branco entre eles:

```
HBN-Readback: 0031
HBN-Human-Authorization: Mauricio (Luis Mauricio Junqueira Zanin)
HBN-Token-FP: 34a7f2f9
```

### A7. Leveza + via mais simples (P-CAND-01)

**PASSA com ressalva marginal.**

O guard é mínimo e direto: `git show` do blob + `wc -l` + `awk` para contar itens numerados na PARTE A (`assert-frontdoor.sh:48-75`). Entrada bloqueante no runner (`hbn-guards-runner.sh:62`). Documentação alinhada (`guards/README.md:87-93`).

`core/role-cards.md` (32 linhas) funciona como **ponteiro**: três cartões curtos com `SPEC COMPLETA:` apontando para `core/orchestrator-profile-spec.md` e `core/roles-assignment-spec.md` sem colar o corpo das specs.

Alternativa mais robusta (fora do escopo desta onda): teto por **bytes/caracteres** ou extensão do scanner `readlist_scan` (`run-guard-tests.sh:1638-1653`) para incluir `core/role-cards.md`. A onda 0031 já rastreia hardening G-KNOW em onda própria; o bypass por conteúdo denso em poucas linhas físicas é dívida análoga, não bloqueador desta entrega.

## Parte B — EXTRA G-FRONTDOOR

| critério | veredito |
|---|---|
| ausente/ilegível → BLOCK | ✓ |
| >140 linhas → BLOCK | ✓ |
| read-list >6 → BLOCK | ✓ |
| válido → PASS | ✓ |
| fail-closed | ✓ |
| cartões ponteiros sem duplicar specs | ✓ |
| escopo respeitado | ✓ |
| 160/160 + B1–B25 | ✓ |
| trailers contíguos | ✓ |

## Parte C — vetores finos Cursor

### C1. Método de contagem do teto de 140 linhas

**Mecanismo** (`assert-frontdoor.sh:48-51`):

```bash
LINE_COUNT="$(printf '%s\n' "$CONTENT" | wc -l | tr -d '[:space:]')"
```

- Conta **linhas físicas** do blob git (`git show`), não linhas não-vazias nem bytes.
- `printf '%s\n'` normaliza o blob antes do `wc -l` (comportamento estável com/sem newline final).
- O teste negativo adiciona 130 linhas físicas extras (`run-guard-tests.sh:1618`) — confirma que o teto é por contagem de linhas, não por tamanho.

**Bypass de espírito confirmado (marginal):** repo efêmero com cartão Orquestrador contendo ~80 KB em **uma única linha física** (`FAZ: XXXXX…`) → arquivo com **11 linhas físicas / 80.200 bytes** → guard **PASSA** (`rc=0`). O espírito anti-monolito (contexto mínimo) pode ser furado sem furar a contagem mecânica. Não invalida a onda; é dívida de hardening futura (teto por bytes ou limite por campo FAZ/ENTREGA).

### C2. Read-list: paths corretos e existência no disco

**Itens da PARTE A** (`core/role-cards.md:7-12`):

| # | entrada | existe no disco? |
|---|---|---|
| 1 | `.hbn/relay/STATE.md` | ✓ |
| 2 | O readback ativo apontado no STATE | meta (dinâmico; STATE → `0031-porta-da-frente-cartoes.json` ✓) |
| 3 | `core/role-cards.md` | ✓ |
| 4 | `.hbn/knowledge/0001-comandos-atomicos-copiaveis.md` | ✓ |
| 5 | `.hbn/knowledge/0002-entrega-operacional-minimalista.md` | ✓ |
| 6 | `.hbn/knowledge/0023-area-temporaria-e-fixtures-efemeras.md` | ✓ |

**Ponteiros dos cartões:** `core/orchestrator-profile-spec.md` ✓ · `core/roles-assignment-spec.md` ✓.

**Sem ponteiro-morto** no estado atual (contraste com G-KNOW inverso rastreado em S3.1).

**Lacuna de enforcement:** G-FRONTDOOR **não** verifica existência dos paths citados — só presença, linhas e contagem. O teste `readlist_scan` permanece restrito a `agents/role-templates.md` + 4 specs core (`run-guard-tests.sh:1655-1660`), **não** inclui `core/role-cards.md`. Hoje os seis paths concretos existem; uma knowledge futura apagada ou renomeada não seria bloqueada por G-FRONTDOOR até hardening.

### C3. Método de contagem da read-list

`awk` em `assert-frontdoor.sh:54-65`:

- Ativa contagem só entre heading `## PARTE A …` e `## PARTE B …`.
- Incrementa em linhas que casam `^[[:space:]]*([0-9]+\.|-|\*)[[:space:]]+`.
- Os 6 itens atuais usam numeração `1.` … `6.` — contagem = 6, alinhada ao handoff.

Itens fora da PARTE A ou sem o padrão numerado/bullet **não** entram na contagem (cartões na PARTE B são ignorados).

## Marginais

1. **M-01 — monólito por linha física única:** ~80 KB em 11 linhas passa G-FRONTDOOR; furar espírito sem furar contagem (C1).
2. **M-02 — existência de paths não guardada:** G-FRONTDOOR não bloqueia ponteiro-morto na read-list; scanner `readlist_scan` não cobre `role-cards.md` (C2). Estado atual íntegro.
3. **M-03 — item 2 dinâmico:** readback ativo é meta-instrução, não path fixo — correto por design; exige leitura do STATE em runtime.
4. **M-04 — sandbox:** suites vermelhas sem permissão `all` por falha ao `git init` em tmp — ambiental, não defeito de lógica.

Nenhum marginal é bloqueador para selagem desta onda dado o escopo declarado do readback 0031 (`out_of_scope`: hardening G-KNOW / P-CAND-04).

## Confiança

**90/100** — evidência mecânica forte (160/160, B25, escopo, trailers, A1/A2 reproduzidos, paths existentes). −10 pelos vetores finos M-01 (bypass por bytes em poucas linhas) e M-02 (sem guard de existência na read-list da porta da frente).

---

Assinatura: **cursor** (auditor cruzado, família distinta de Codex e Gemini)  
Data: **2026-06-16T13:27:43-03:00**
