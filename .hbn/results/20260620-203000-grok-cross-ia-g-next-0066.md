---
path: .hbn/results/20260620-203000-grok-cross-ia-g-next-0066.md
id-global: 20260620-203000-grok-cross-ia-g-next-0066
tipo: audit-result
autor: grok
familia: xAI
veredito: "APROVA_0066: SIM"
arvore: fronteira
created_at: "2026-06-20T20:30:00-03:00"
---

SOU: grok · familia xAI · papel auditor

## 1. git rev-parse main
Comando:
cd /Users/macbookpro/Projetos/usehbn && git rev-parse main
Saida:
4db692876381a0d7909985c8500d999f2e677b04
Confirmado: main INTOCADA exatamente no hash exigido (Truth Barrier via comando+saida).

## 2. guards/assert-next-checkpoint.sh — enums, REQUIRED, bloco_ref, exatamente UM proximo_ponto
Arquivo lido: guards/assert-next-checkpoint.sh

Enums (linhas 89-91):
```89:91:guards/assert-next-checkpoint.sh
ACTOS = {"implementacao", "cross-audit", "hearback", "selagem", "freeze", "fim"}
GATES = {"nenhum", "hearback_humano"}
STATUS = {"pendente", "em_curso", "concluido"}
```

REQUIRED (linha 92):
```92:92:guards/assert-next-checkpoint.sh
REQUIRED = {"passo", "ato", "destino", "gate", "bloco_ref", "status"}
```

Exatamente UM proximo_ponto top-level (linhas 202-204):
```202:204:guards/assert-next-checkpoint.sh
    if len(occurrences) != 1:
        fail(f"{state_ref}: esperado exatamente 1 proximo_ponto top-level; encontrados {len(occurrences)}.")
        return {}
```

Regra de bloco_ref (deve existir como blob no indice/HEAD) — linhas 288-289 (e contexto 284-289):
```284:289:guards/assert-next-checkpoint.sh
        if bloco_ref == "nenhum":
            fail(f"{state_ref}: proximo_ponto.bloco_ref so pode ser 'nenhum' quando ato=fim.")
        elif not valid_version_path(bloco_ref):
            fail(f"{state_ref}: proximo_ponto.bloco_ref nao e caminho versionado seguro: {bloco_ref}")
        elif not blob_exists(bloco_ref):
            fail(f"{state_ref}: proximo_ponto.bloco_ref inexistente no indice/HEAD: {bloco_ref}")
```
blob_exists usa git cat-file -e no ref staged (:) ou HEAD: conforme modo (local vs CI com HBN_DIFF_BASE). Nunca working tree.

O guard so opina quando .hbn/relay/STATE.md aparece em diff --name-only --diff-filter=AM (staged local) ou range (CI).

## 3. G-NEXT no runner
Comando:
cd /Users/macbookpro/Projetos/usehbn && grep -n assert-next-checkpoint guards/hbn-guards-runner.sh
Saida:
105:    "assert-next-checkpoint.sh"

Confirmado no array GUARDS do runner (linha 105 do arquivo).

## 4. bash guards/hbn-guards-runner.sh
Comando:
cd /Users/macbookpro/Projetos/usehbn && bash guards/hbn-guards-runner.sh 2>&1
Saida (trecho final):
[hbn-guards/assert-next-checkpoint] ✓ STATE nao foi adicionado/modificado neste diff — G-NEXT nao opina.
...
[hbn-guards] Todos os guards passaram.
Todos os guards passaram (exit 0).

## 5. bash guards/tests/run-guard-tests.sh
Comando:
cd /Users/macbookpro/Projetos/usehbn && bash guards/tests/run-guard-tests.sh 2>&1
Saida (resumo final):
== assert-next-checkpoint (G-NEXT) ==
  ✓ next: STATE com proximo_ponto bem-formado passa (esperado: pass)
  ✓ next: STATE sem proximo_ponto → BLOCK (esperado: block)
  ✓ next: ato fora do enum → BLOCK (esperado: block)
  ✓ next: destino nao-canonico → BLOCK (esperado: block)
  ✓ next: bloco_ref inexistente → BLOCK (esperado: block)
  ✓ next: proximo_ponto duplicado → BLOCK (esperado: block)
...
== resumo: 233 passaram, 0 falharam ==
SUÍTE VERDE — todos os casos-ruins BLOQUEADOS, todos os casos-bons passam.
---
exit_code: 0

## 6. bash guards/tests/adversarial-battery.sh
Comando:
cd /Users/macbookpro/Projetos/usehbn && bash guards/tests/adversarial-battery.sh 2>&1
Saida (trecho final relevante):
B63 STATE sem proximo_ponto                          | G-NEXT   | BLOQUEADA ✓
B64 proximo_ponto.ato fora do enum                   | G-NEXT   | BLOQUEADA ✓
B65 destino nao-canonico                             | G-NEXT   | BLOQUEADA ✓
B66 bloco_ref inexistente                            | G-NEXT   | BLOQUEADA ✓
B67 proximo_ponto duplicado                          | G-NEXT   | BLOQUEADA ✓

BATERIA VERDE — toda burla documentada foi BLOQUEADA pelo guard correspondente.
---
exit_code: 0

Casos B cobrindo G-NEXT confirmados (B63-B67).

## 7. readback 0066
Arquivo: .hbn/readbacks/0066-w-next-g-next.json (lido completo)

Citações chave:
```7:8:.hbn/readbacks/0066-w-next-g-next.json
  "status": "implemented_pending_cross_audit",
  "activation_status": "PROPOSED_UNTIL_CROSS_AUDIT",
```
```6:6:.hbn/readbacks/0066-w-next-g-next.json
  "track": "safe_track",
```
```4:5:.hbn/readbacks/0066-w-next-g-next.json
  "agent_id": "codex",
  "implementador_id": "codex",
```
scope.files_allowed (linhas 21-31) inclui exatamente os artefatos entregues:
guards/assert-next-checkpoint.sh, guards/hbn-guards-runner.sh, guards/tests/*, .hbn/messages/*despacho*, .hbn/readbacks/0066*, .hbn/relay/STATE.md, REGISTRY.md, .hbn/attestations/*.

Nenhum files_forbidden tocado (guards/data/**, core/**, etc. permanecem intocados no commit 98a680a).

## 8. Busca ativa por regressão / falso-verde / drift / falta de teste / burla
- Truth via disco: commit de entrega 98a680a ("feat: add G-NEXT checkpoint guard") tocou exatamente os arquivos do files_allowed (stat via git show). main permanece 4db6928 (git rev-parse).
- No commit de entrega, STATE.md carregava proximo_ponto válido (citação via git show):
```15:21: (via git show 98a680a:.hbn/relay/STATE.md)
proximo_ponto:
  passo: "cross-audit do G-NEXT (readback 0066)"
  ato: cross-audit
  destino: human
  gate: hearback_humano
  bloco_ref: .hbn/messages/20260619-140000-opus-4-8-despacho-w-next-g-next.md
  status: pendente
```
  (bloco_ref apontava para o despacho .md conforme spec do despacho; blob existia no commit.)
- Parser G-NEXT: valida front-matter, exatamente 1 proximo_ponto top-level bloco (não inline), REQUIRED exatos, enums, destino via mapa+hardcoded {codex,human,nenhum}, bloco_ref existe via git cat-file no indice/HEAD (nunca WT), valid_version_path anti-traversal.
- Cobertura: run-guard-tests cobre bom + 5 ruins (ausência, ato inválido, destino não-canonico, bloco inexistente, duplicado) → 6 checks. Adversarial B63-B67 replica as burlas e exige BLOQUEIO.
- Bypass intencional (guard_check_bypass em G-NEXT + todos): exige HBN_GUARDS_BYPASS=1/GLASSWING + nota ADICIONADA em .hbn/bypasses/ no MESMO diff (F-10). Documentado, não é regressão; adversarial cobre o padrão para outros guards e common.sh é herdado.
- Possível "rename bypass" investigado (repro em repo temp): git mv da canonical .hbn/relay/STATE.md → G-NEXT rc=0 (skips, pois diff-filter=AM não casa R). Porém:
  - Remove o path canônico que TODOS os guards (scope, role-family, orq-*, etc.) resolvem via STATE_PATH fixo.
  - Outros guards falhariam (STATE ausente/ilegível).
  - Não constitui "STATE muda sem proximo_ponto valido e commit passa" de forma útil — repo fica quebrado para o modelo inteiro.
  - Sem repro de bypass prático.
- Drift de escopo: commit 98a680a e readback 0066 respeitam files_allowed/forbidden. Nenhuma alteração em guards/data/auditor-families.txt (mapa intocado, como exigido).
- Falso-verde: testes usam fixtures herméticas (mktemp git repo); guards leem só indice/HEAD. Runner em CI usaria range. Nenhuma evidência de skew ou working-tree leak no G-NEXT (diferente de E-FECH fixados em guards anteriores).
- Faltas de teste: suíte + bateria cobrem os casos exigidos no despacho/readback; 233/0 e BATERIA VERDE batem os critérios.
- Outras buscas (grep por STATE_PATH, proximo_ponto, HBN_DIFF_BASE em guards/): consistentes; G-NEXT usa o mesmo padrão C3 de outros guards (assert-copy-block etc.).
- Nenhum caminho encontrado em que .hbn/relay/STATE.md é adicionado/modificado no diff (AM no staged ou range) sem proximo_ponto bem-formado e o commit passa.

Nenhuma regressão, falso-verde, drift de escopo, falta de teste ou burla reprodutível não-intencional encontrada. G-NEXT implementado conforme spec, tests verdes, main intocada, escopo respeitado, ativação PROPOSED_UNTIL_CROSS_AUDIT honrada.

APROVA_0066: SIM
