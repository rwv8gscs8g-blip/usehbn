---
titulo: "Grok — cross-audit P-CAND-04 área temporária"
tipo: result
status: active
temperatura: frio
id-global: 20260616-204530-grok-cross-ia-p-cand-04
path: .hbn/results/20260616-204530-grok-cross-ia-p-cand-04.md
autor: grok
created_at: "2026-06-16T20:45:30-03:00"
---

APROVA_0033: NAO

Bloqueadores (Truth Barrier — arquivo:linha ou comando+saida; conferido no disco, sem confiar em relatos):

- guards/tests/adversarial-battery.sh:424: B26 "arquivo staged em scratch/" → try_burla registra "PASSOU ✗ (FALHA DA ONDA)" (guard retornou 0, nao bloqueou). Setup: mkdir -p scratch; echo > scratch/segredo.txt; git add scratch/segredo.txt (sem -f, mas no contexto do mk_repo); bash .../assert-scratch-lock.sh ; echo $?
- guards/tests/adversarial-battery.sh:435: B27 "symlink staged em scratch/" → "PASSOU ✗ (FALHA DA ONDA)". ln -s ../core scratch/link; git add scratch/link; assert-scratch-symlink.sh → rc=0.
- guards/assert-scratch-lock.sh:20-33 (e analog em symlink.sh:14-45; via scratch_diff_files | guard_paths_to_version_paths): quando insumo .hbn/active-version ausente/ilegivel, a conversão falha (common.sh:146: guard_active_version_rel || return 1), o pipe produz lista vazia, while não itera, FAIL=0, guard_ok "Nenhum path proibido...", exit 0 → fail-OPEN. Live repro (A5):
  (cd /Users/macbookpro/Projetos/usehbn && rm -f .hbn/active-version; bash guards/assert-scratch-lock.sh 2>&1) → "[hbn-guards/assert-scratch-lock] ✓ Nenhum path proibido staged sob scratch/."
  (restaurado com git checkout -- .hbn/active-version)
- guards/tests/run-guard-tests.sh + adversarial-battery.sh: execução atual → "SUÍTE VERMELHA — nenhum guard pode ser ativado no runner (ADR-020 Decisão 2)." e "BATERIA VERMELHA — 6 burla(s) PASSARAM." (incl B26/B27 + B4/B15/B20/B21). Contradiz alegado "165/165; B1-B28" (ver STATE.md:18, handoff-p-cand-04.md:15,40, readback 0033:58).
- scratch guards (lock/symlink) não robustos ao modelo de versão ativa (prefixo != "."): no harness mk_repo dos adv-tests (que usa active-version/ canonical-root de cenários adv-cr-*), o f após guard_repo_path_to_version_path vira "versao_.../scratch/..." (ou vazio), case "scratch/*" (lock.sh:28, symlink.sh:38) não casa → burla passa. (B28 passou porque usa caminho diferente, grep no conteúdo do .gitignore via git show ":${GITIGNORE_REPO_PATH}".)

Verificações positivas (todas conferidas no disco via git/read/shell):

A1. G-SCRATCH-LOCK (live stage+reset, sem solto, per knowledge 0023 .hbn/knowledge/0023-area-temporaria-e-fixtures-efemeras.md:27-28):
  - stage scratch/segredo.txt → BLOQUEIA: "[hbn-guards/assert-scratch-lock] ✗ COMMIT BLOQUEADO motivo: Path staged proibido sob scratch/: scratch/segredo.txt..."
  - stage scratch/README.md → PASS: "✓ Nenhum path proibido staged sob scratch/." RC=0
  Comando completo + cleanup executado; final "CLEAN: no test-induced loose scratch or dirty gitignore"; ls scratch/ → apenas README.md

A2. G-SCRATCH-SYMLINK (live):
  - ln -s ../REGISTRY.md scratch/x; git add -f scratch/x → BLOQUEIA: "✗ COMMIT BLOQUEADO motivo: Symlink staged proibido sob scratch/: scratch/x." RC=1
  (reset + rm -f; clean)

A3. G-SCRATCH-IGNORE (live, edit temporário de .gitignore + reset/checkout):
  - .gitignore staged sem /scratch/ → BLOQUEIA: "perdeu a linha obrigatoria: /scratch/"
  - sem a excecao !/scratch/README.md → BLOQUEIA: "perdeu a excecao obrigatoria: !/scratch/README.md"
  (ambos RC=1; restaurado exato; .gitignore clean no final)

A4. Excecao README: padrao exato "!/scratch/README.md" (gitignore:43), não casa README.md.bak / subdir/README.md etc. Lock adicionalmente barra qualquer scratch/* != README.md (independente de ignore). Nenhum abuso de path ou conteudo arbitrario no README (é o contrato versionado intencional, per scratch/README.md:6 e guards/README.md:60-73). OK.

A6. Escopo: git diff --name-only c4af7bf^..2d4ad86 (p-cand apenas, 5 commits) lista exatamente:
  .gitignore, .hbn/messages/20260616-170500-codex-handoff-p-cand-04.md, .hbn/readbacks/0033-area-temporaria-scratch.json, .hbn/relay/STATE.md, REGISTRY.md, guards/README.md, guards/assert-scratch-*.sh (3), guards/hbn-guards-runner.sh, guards/tests/{run-guard-tests,adversarial-battery}.sh, scratch/README.md
  == files_allowed do readback 0033 (json:19-33; handoff usa timestamp concreto vs placeholder HHMMSS no json — match por padrao). (git diff 4857081..2d4ad86 inclui extras de selagem-s3-2 intercalados, mas onda P-CAND-04 scoped corretamente.)

A7. Sem regressao (runner local vs suite):
  - guards/hbn-guards-runner.sh (tree atual, index limpo): verde. Saida: "Todos os guards passaram." (incl 3 scratch ✓; canonical, scope, frontdoor, exc F-01 etc.)
  - porem suite: run-guard-tests.sh → vermelha; adversarial → BATERIA VERMELHA (6 passadas, B26/B27 entre elas). Runner "verde antes de cada commit" alegado só no relato (handoff:41); execucao atual da bateria reprova. main 4db692876381a0d7909985c8500d999f2e677b04 confirmado (readback:73, rev-parse).

A8. Trailers contiguos: git log --format=... --no-walk dos 5 commits → cada um termina exatamente com:
  HBN-Readback: 0033
  HBN-Human-Authorization: Mauricio (Luis Mauricio Junqueira Zanin)
  HBN-Token-FP: 34a7f2f9
  (sem texto apos; 5/5). OK. (c4af7bf a 2d4ad86)

A9. LEVEZA (ref P-CAND-01): 3 guards sao simples e de responsabilidades distintas (lock deny-by-default, symlink 120000, protecao do .gitignore). Baixa redundancia. Porem: duplicam logica de diff/paths em vez de compor helper mais forte; o case "scratch/*" pos-remap de versao e fragil (causa dos fails em B26/B27 no harness). Nao e "a coisa mais simples" possivel (um guard combinado lock+symlink + checagem ignore no mesmo reducer seria menor), mas separacao ajuda leitura. Integracao com active-version (M-A) incompleta nesta onda. Marginal.

EXTRA (vetor residual): symlink runtime (nao staged) scratch/escape -> ../core :
  - criado, guards executados (lock/sym/ignore): todos 3 "✓" PASS (correto, pois nao em --cached).
  - rm -f ; clean.
  Risco real? Sim: processo em execucao (testes, scripts, REPL) pode criar symlink em scratch/ apontando para fora (leitura/escrita de ../.env, ../core, ../.hbn etc conforme perms do usuario). Guards G-SCRATCH so atuam em stage/CI range (VC pollution). README proibe ("Symlinks em `scratch/` que apontem para fora de `scratch/` sao proibidos."), mas sem enforcement runtime. Aceito nesta onda (out_of_scope explicito no readback 0033:62 "Bloqueio total deny-by-default da zona livre (proxima onda)"). Recomendacao: (1) nao usar scratch/ para nada sensivel; (2) proxima onda (deny) considerar assert runtime leve ou forcar $TMPDIR externo + docs; (3) .gitignore nao impede criacao de symlinks.

Outros fatos conferidos (comando+saida / arquivo:linha):
- 5 commits sobre 4857081, HEAD=2d4ad86, branch=proposta/reestruturacao-m-a-s0 (git log --oneline 4857081..HEAD + rev-parse).
- readback 0033 (json:1-78): readback_id="0033-area-temporaria-scratch", files_allowed exato para P-CAND (sem brainstorm/core), out_of_scope inclui deny/hardening, human_status=confirmed, authorization nota "Nao autoriza tocar main...".
- .gitignore:42-43: "/scratch/\n!/scratch/README.md" (exato).
- scratch/README.md:1-9 (contrato + "use stage + reset conforme knowledge 0023").
- guards/README.md:64-73 (documenta os 3 G-SCRATCH).
- runner insere os 3 (hbn-guards-runner.sh:54-56).
- STATE.md:52 readback_ativo=0033...; 5: "P-CAND-04 ENTREGUE"; 18: "165/165 + B26-B28" (alegado, nao confere na exec); 14: proxima_acao="Cross-audit P-CAND-04".
- handoff 170500:14-18 repete 165/165, "main intocada", "trailers contiguos".
- 165/165 e B26-28 so em relatos/STATE/handoff (nao na execucao atual da suite).
- active-version: "." (1 linha).
- Nao tocamos main, zero commit, zero --no-verify, so leitura + stage+reset temporarios + rm para probes.
- .hbn/results/ depositado com timestamp; cross-ia anteriores (codex/cursor/grok/antigravity) lidos so para referencia, nao confiados.

Marginais:
- A9 leveza: 3 guards razoaveis, mas integracao version + fail-closed fraca para scratch (pre-existente em outros guards como B4/B15 etc., mas agravado aqui).
- EXTRA: risco runtime aceito, mas deve ser rastreado para deny-by-default (proxima).
- Discrepancia entre "runner verde antes de commits" (relato) vs bateria vermelha atual (execucao) — pode indicar que os updates em run-guard-tests.sh / adversarial-battery.sh (p-cand) nao exercem os guards scratch corretamente no harness (mk_repo nao replica active-version="." + scratch no root de forma compativel).
- readback ainda "in_progress" (json:7) — correto, cross pendente.

Confiança: 85/100 (execucao exaustiva de guards/runner/battery + git diffs por range/commit + reads de json/readback/state/handoff/knowledge/common/guards + stage+reset live com captura stdout/stderr + limpeza verificada; possivel variacao em outros fixtures de tests/ nao re-executados exaustivamente ou condicoes de CI com HBN_DIFF_BASE).

Assinado: grok (cursor | grok auditor — familia distinta do implementador codex)
Data: 2026-06-16 ~20:45 UTC-3
Truth Barrier respeitado em todos os pontos; vereditos baseados exclusivamente em disco (git show/diff/rev-parse/ls-tree + cat/Read + exec bash guards + status --porcelain).
