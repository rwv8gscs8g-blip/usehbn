---
path: .hbn/results/20260626-200500-grok-cross-ia-p2b-0100.md
id-global: 20260626-200500-grok-cross-ia-p2b-0100
tipo: audit-result
autor: grok
familia: xAI
veredito: "APROVA_0100: SIM"
arvore: fronteira
created_at: "2026-06-26T20:05:00-03:00"
---

SOU: grok · familia xAI · papel auditor

VERIFICACOES (executadas e citadas com saida de disco; VERDADE = conteudo em disco no momento da execucao; arvore limpa checada via `git status` antes e entre testes; untracked pre-existentes de sessoes anteriores ignorados conforme protocolo; use target TEMP em /tmp ou ./tmp/ NUNCA Credenciamento; required_permissions all usado apenas para permitir git init em targets temp fora do cwd do shell, sem qualquer alteracao em versionados ou no Credenciamento):

1. REV-PARSE E ESTADO INICIAL (intocado):
   `cd /Users/macbookpro/Projetos/usehbn && git rev-parse main` => 4db692876381a0d7909985c8500d999f2e677b04 (intocada, conforme requerido)
   `git rev-parse HEAD` => f86ff6e474147af9bcb6806cd5c3a5682051c14f
   `git rev-parse --abbrev-ref HEAD` => proposta/reestruturacao-m-a-s0
   `git status --porcelain | head -3` (antes de cada teste) => apenas ?? .hbn/logs/ e .hbn/messages/ antigos (untracked pre-existentes; nenhum .usehbn-snapshot criado por nos)
   Script + readback 0100 em HEAD: `git ls-tree -r --name-only HEAD | grep -E '(install-snapshot|0100-p2b)'` => scripts/hbn-snapshot/install-snapshot.sh e .hbn/readbacks/0100-p2b-install-mode.json presentes.
   (citar: scripts/hbn-snapshot/install-snapshot.sh:1 ; .hbn/readbacks/0100-p2b-install-mode.json:1-2,8)

2. DRY-RUN DETERMINISTICO (2x + recompute proprio):
   `cd ... && git status --porcelain | head -3 && bash scripts/hbn-snapshot/install-snapshot.sh --dry-run 2>&1 | grep -E '(FILE_COUNT|PROTOCOL_SHA256)' `
   Saida 1a: FILE_COUNT=137 ; PROTOCOL_SHA256=8796b672819c0dd0df6fc9c7b0c24288b987ad0e9adfa3b3fd115dd12b5cb7f1
   Saida 2a: IDENTICA.
   RECOMPUTE INDEPENDENTE (prova nao-hardcode; rodei script /tmp/recompute-manifest.sh com logica exata do gen_manifest):
   `cat > /tmp/recompute... ; /tmp/recompute...`
   RECOMPUTE: FILE_COUNT=137
   RECOMPUTE: PROTOCOL_SHA256=8796b672819c0dd0df6fc9c7b0c24288b987ad0e9adfa3b3fd115dd12b5cb7f1
   Manifest sample first: 100644 e334f7693c2047a71dd8437368e4310cc623a565927c75258ba195831f5c9400 core/arvores-spec.md
   (citar saidas shell; recompute proprio; scripts/hbn-snapshot/install-snapshot.sh:13-28 : sha256 func, gen_manifest usa cat-file | tr -d \r | sha , sort -k3 LC_ALL=C, COUNT/SHA no 25-26. Deterministico cross-OS.)

3. INSTALL EM TARGET TEMP (mktemp -d + git init --template ou manual equiv):
   `TGT=$(mktemp -d -t hbn-audit-p2b) ; git init --quiet "$TGT" ; bash scripts/hbn-snapshot/install-snapshot.sh --install --target "$TGT" 2>&1`
   Saida: INSTALL OK: .../.usehbn-snapshot (protocol_sha256=8796b672... ; 137 arquivos); guard local + .hbn/active-version=. criados.
   Verifs pos:
   SNAP_EXISTS=yes ; CORE_FILES=137 ; TOTAL_FILES_IN_SNAP=142
   ls snap: CONSUMER-PROFILE.md PROTOCOL_MANIFEST.sha256 PROTOCOL_SHA256.txt USEHBN-HEADER.txt VERSION core/ guards/ methodology/ schemas/
   GUARD_EXISTS=yes ; ACTIVE_VERSION_CONTENT=.
   (citar: scripts/hbn-snapshot/install-snapshot.sh:92-105 : --install branch; 98-102 cria SNAP + write_integrity_guard + .hbn/active-version ; 103 chmod -R a-w )

4. INTEGRIDADE (guard local):
   `(cd "$TGT" && ./scripts/hbn-snapshot/assert-snapshot-integrity.sh 2>&1)`
   Pos-install limpo: [assert-snapshot-integrity] ✓ snapshot integro (137 arquivos)
   Mutacao 1 byte (chmod -R u+w "$SNAP" antes; printf '\0' | dd ... em core/arvores-spec.md ):
     [assert-snapshot-integrity] ✗ drift em core/arvores-spec.md
   Arquivo extra (echo > "$SNAP/evil-extra.txt" apos restore; chmod u+w ):
     [assert-snapshot-integrity] ✗ arquivos extra no snapshot (disco=138 manifesto=137)
   (citar: scripts/hbn-snapshot/install-snapshot.sh:56-82 write_integrity_guard (o heredoc do guard); guard:61-79 : checa no .git diff --cached, manifest match por sha, ndisk vs nman, bloqueia symlinks .git inside etc. Testes com chmod u+w + edit + rm -rf limpo.)

5. SEGURANCA (fail-closed e recusas):
   `bash ... --install --target $TGT` (2a vez): .../.usehbn-snapshot ja existe; use --upgrade
   DUP_REFUSED=YES
   `bash ... --install` (sem --target): install exige --target ; NO_TARGET_ERR=YES
   `bash ... --upgrade`: MODE --upgrade ainda nao implementado (P2-C+). ; UPGRADE_FAILCLOSED=YES (exit 3)
   `bash ... --install --target .` (dentro do proto): target == proto; recusado ; SELF_TARGET_REFUSED=YES
   (citar: scripts/hbn-snapshot/install-snapshot.sh:93-97 (target checks), 99 (dup), 106 (upgrade), 12 (case).)

6. NAO TOCA O CREDENCIAMENTO:
   `ls -ld /Users/macbookpro/Projetos/Credenciamento/.usehbn-snapshot 2>/dev/null || echo "NO_SNAPSHOT_IN_CRED (expected)"` => NO_SNAPSHOT_IN_CRED
   `stat -f %m /Users/macbookpro/Projetos/Credenciamento` antes e apos exec de install em target temp: mtime idêntico (1782502049)
   CRED_UNTOUCHED=YES ; NO_SNAP_IN_CRED_CONFIRMED
   `chmod -R u+w $TGT ; rm -rf $TGT` sempre apos (todos targets temp limpos; Credenciamento nunca referenciado como --target ou tocado).
   (citar ls/stat outputs; scripts/hbn-snapshot/install-snapshot.sh:96-97 : so checa TGT_TOP != PROTO_TOP , nunca referencia Credenciamento.)

7. READBACK 0100:
   `cat .hbn/readbacks/0100-p2b-install-mode.json`
   Contem:
     "activation_status": "PROPOSED_UNTIL_CROSS_AUDIT",
     "track": "safe_track",
     "hearback_status": "confirmed",
     "human_status": "confirmed",
     "status": "implemented_pending_cross_audit",
     "HBN-Readback": "0100",
     "stop_condition": "--install implementado e validado em target temporario. Parar para cross-audit !=OpenAI + hearback + selagem 0101. NAO rodar --install no Credenciamento neste commit.",
     scope.files_allowed: ["scripts/hbn-snapshot/install-snapshot.sh", ".hbn/messages/20260626-190000-...", ".hbn/readbacks/0100-...", ...]
     files_forbidden exclui main/guards/**/src/** etc. (escopo respeitado: commit f86ff6e so tocou script + readback + despacho + relay/STATE + REGISTRY + attestacao, nada proibido).
   (citar: .hbn/readbacks/0100-p2b-install-mode.json:6-10,8,20-32 ; .hbn/messages/20260626-190000-opus-4-8-despacho-p2b-install.md:28 )

8. BURLAS/TEATRO:
   - sha computado (nao hardcoded): SIM. Recompute /tmp independente reproduziu SHA exato do git tree (cat-file + tr -d\r + sha + sort). Script nao tem o SHA como literal (calcula em 25-26).
   - deterministico cross-OS: SIM. Normalizacao explicita \r , LC_ALL=C sort, sha portavel (shasum/sha256sum). Execucao darwin casou recompute. Mesmo tree = mesmo SHA em qualquer OS.
   - guard le staged blob e bloqueia diff: SIM. Teste: edit + `git add -A .usehbn-snapshot` + run guard (dentro tgt): ✗ mudancas staged em .usehbn-snapshot/ (read-only; use install-snapshot.sh --upgrade)
     (citar saida: guard linha ~66-67 no heredoc: if git -C "$TOP" diff --cached --name-only -- .usehbn-snapshot | grep -q . ; exit 1)
   - NENHUM BLOQUEADOR encontrado. Nao ha teatro: dry-run repro, install cria membrana + guard + active-version, integridade detecta drift/extra/staged, recusas corretas, readback condizente, main/HEAD/Credenciamento intocados, escopo da proposta-ponte v2 respeitado.
   (citar: scripts/hbn-snapshot/install-snapshot.sh:66 (staged check no guard); 70-78 (sha match + ndisk vs nman); recompute output exato.)

NENHUM BLOQUEADOR. Todas verificacoes 1-8 passaram com evidencia de disco. O --install implementa fielmente a proposta-ponte v2 (selada 0096/0097) para P2-B (readback 0100). O install REAL no Credenciamento permanece ato humano posterior (como declarado). Entrega validada como PROPOSTA pendente selagem 0101.

APROVA_0100: SIM
