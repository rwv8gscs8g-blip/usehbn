---
titulo: "Auditoria cruzada — Readback 0004 (jaula definitiva do orquestrador)"
tipo: result-cross-ia
status: emitido
temperatura: quente
path: .hbn/results/20260711-162330-grok-cross-ia-jaula-0004.md
created_at: "2026-07-11T16:23:30-03:00"
autor: grok
familia: xAI
natureza: nativo
---
SOU: grok · familia xAI · papel auditor

Refs: readback_ref=.hbn/readbacks/0004-jaula-orquestrador.json · hearback_ref=.hbn/hearbacks/0004-jaula-orquestrador-hearback.json

# Parecer — auditoria cruzada 0004 (jaula do orquestrador)

Auditor: grok · família xAI · papel AUDITOR CRUZADO.  
Escopo: diff **STAGED** sobre HEAD `51ce570`. Não implementei, não corrigi, não fiz staging, não commitei. Não li pareceres de outros auditores desta onda antes de emitir este.

## §0 Preflight (citado)

| Check | Saída observada |
|---|---|
| `cat .hbn/active-version` | `versao_3_0_0` |
| `git rev-parse --short HEAD` | `51ce570` |
| `git diff --cached --stat` | `22 files changed, 1388 insertions(+), 17 deletions(-)` |

Insumos lidos: readback `0004-jaula-orquestrador.json` (status proposto, F-01 / PROPOSED_UNTIL_CROSS_AUDIT), hearback gate confirmado+assinado, aditivo-1 confirmado+assinado (ampliação de `generate-manifest.sh`), dispatch `20260711-161600-claude-opus-4-8-dispatch-jaula-autocontido-codex.md`.

Staging cirúrgico: `ci-entry.sh` (Onda-0b) permanece **fora** do index; glacier/`versao_0_3_x` não entram no staged. Escopo alinhado ao files_allowed (+ aditivo-1). `INDEX.md` permitido mas não alterado (não-bloqueante).

## §1 Achados por item

### 1.1 `guards/lib/common.sh` — bypass por classe

**Veredito: CONFORME (endurecimento; CI Onda-0b não enfraquecido).**

- `guard_check_bypass`: se `classe != documental` (incluindo ausente/desconhecida) → `return 1` sempre, com aviso; env `HBN_GUARDS_BYPASS`/`GLASSWING_BYPASS` ignorados para estrutural.
- Documental: exige nota staged em `.hbn/bypasses/` **e** `hearback_ref` apontando para hearback humano com `status` confirmado/confirmado **e** `gate.assinatura` ou `signed_by`.
- Classe ausente no MANIFEST → tratada como estrutural (fail-closed).
- `hbn_ci_range_mode` / `guard_paths_to_version_paths` / exigência de sinal CI para range mode **preservados**; o path de notas em CI ganhou `guard_paths_to_version_paths` (fortalece resolução, não afrouxa).
- Onda-0b em `ci-entry.sh` (cd invariante + onboarding de shims) está **unstaged** e intocada pelo staged da jaula — correto e não enfraquecida neste pacote.
- Residual menor: ramo “legado” permite bypass documental só com nota se a raiz ativa **não** tiver `MANIFEST.yaml`; a versão quente classificada **não** entra nesse ramo.

### 1.2 `guards/hooks/pre-write-gate.sh`

**Veredito: CONFORME.**

- Decisão antes da escrita (exit 2 em deny).
- Resolve path via `abspath` + `realpath` do dirname; contenção com `os.path.commonpath`.
- Orquestrador: só ledger (`HBN_JAULA_LEDGER`); Bash só se comando delegar a `jaula-sh`; recusa `--no-verify` / `chmod +w`.
- Symlink no alvo do ledger recusado; escape via symlink de diretório coberto por realpath (JT-09 verde).
- Residual: settings Claude usa comando relativo `bash versao_3_0_0/guards/hooks/pre-write-gate.sh` (depende de cwd = raiz do repo).

### 1.3 `scripts/jaula/jaula-sh`

**Veredito: CONFORME (allowlist positiva).**

- Default deny (`*)`); allowlist explícita de leitores + `git` RO + `ledger-write`.
- Tokens com `>`, `<`, `|`, `;`, `&`, backticks, `$(`, etc. recusados → redirections/heredoc-as-token bloqueados.
- `tee|dd|ln|python|pip|chmod|chflags|mount|bash|sh|zsh|…` negados por nome; `git commit|push|tag|reset|update-ref` fora da sub-allowlist RO.
- Residual de cobertura de teste: JT-14 rotula “heredoc” mas exercita token de redirection/`tee|dd|ln`; o mecanismo de deny_token cobriria `<<` se passado como token.

### 1.4 `spawn-orchestrator.sh` + `gen-orchestrator-handoff.sh`

**Veredito: CONFORME no essencial; resíduos operacionais.**

- Atestação = sha256 do bundle `{BOOT.md, jaula-sh, read-list-canonica.txt}` calculado sobre a árvore canônica; `jaula_verify_handoff_attestation` compara o hash declarado no handoff com o recomputado — **não** confia no texto narrativo (JT-12 verde).
- Spawn recusa `ATTESTATION_OPERATOR: PENDENTE` e recusa hash divergente.
- Habitat: worktree detach + `chmod -R a-w` no repo; ledger gravável fora; `GIT_ASKPASS=/usr/bin/false`, `GIT_TERMINAL_PROMPT=0`, `GIT_CONFIG_GLOBAL=/dev/null`; PATH mínimo com `jaula-sh`.
- Residuais: (a) `core.hooksPath` para hooks RO **não** é setado no spawn (lacuna vs spec §4(a.1)); (b) worktree pode herdar remotes do repo pai — push mitigado pelo wrapper/allowlist e ASKPASS, mas não por remoção física de remote; (c) com TCB dirty (pré-commit), gen hasheia working tree e verify usa HEAD do worktree — fail-closed esperado até o commit.

### 1.5 `scripts/jaula/watchdog.sh`

**Veredito: CONFORME no núcleo fail-closed; heartbeat incompleto vs narrativa JT-13.**

- Canário de escrita no repo → `ALERTA-DERIVA-*` + kill do PID do agente + exit 1.
- Revalida todos os hashes de `BASELINE.sha256` (inclui o próprio `watchdog.sh`) → auto-verificação de hash.
- Artefatos fora do contrato do ledger → quarentena + alerta; contador de negações com limite.
- Residual: não há checagem explícita de **heartbeat temporal** (idade/ausência de pulso); JT-13 automatizado só prova EROFS ao editar path RO, não a ausência de heartbeat.

### 1.6 `usehbn/_lock.py`

**Veredito: CONFORME.**

- No import: resolve `canonical-root` + `active-version`; módulo fora da versão ativa → `RuntimeLockError`; drift do MANIFEST vs baseline → aborta.
- `authorize_write` = chokepoint (path na versão + ator + readback + hearback confirmado/assinado); `run_allowed` sem execução genérica.
- Único `.py` sob `usehbn/` no staged; `assert-runtime-lock.sh` (estrutural) amarra baseline + import.
- Residual: após import bem-sucedido, escrita via `open()` direto ainda é possível se um entrypoint futuro ignorar `authorize_write` — a trava de import não substitui disciplina de API; lint de subprocess ajuda.

### 1.7 BASELINE / HASHES + JT-01..JT-14

**Veredito: CONFORME.**

- `BASELINE.sha256` (16 entradas) e `HASHES.sha256` (4) conferidos byte-a-byte contra o working tree staged: **todos OK**.
- TCB coberto: MANIFEST, runner, common.sh, pre-write-gate, pre-push, shims, HASHES, scripts jaula, `_lock.py`, `.claude/settings.json`.
- JT não são tautológicos no sentido pedido: PASS = violação **falhou** (EROFS/EPERM/deny/hash-mismatch/allowlist), não assert de string simulada. Pontos mais fracos de fidelidade à narrativa (não de falsidade): JT-03/04 via wrapper; JT-13 sem heartbeat; JT-14 sem heredoc literal — ainda assim o mecanismo correspondente existe e bloqueia.

## §2 Evidências de execução (auditor)

```
$ bash versao_3_0_0/guards/tests/assert-no-structural-bypass.sh
[anti-bypass] OK: classes completas; bypass estrutural sempre recusado.
EXIT:0

$ bash versao_3_0_0/guards/tests/jaula-adversarial-battery.sh   # fora de sandbox
== bateria adversarial da jaula ==
  ✓ JT-01 .. ✓ JT-14
BATERIA JT VERDE: JT-01..JT-14 bloqueados; JT-15 documentado.
EXIT:0
(Nota: sob sandbox Cursor, JT-05 falhou por EPERM em mkdir .git/hooks — artefato de sandbox, não do mecanismo; reexecução unrestricted = verde.)

$ bash versao_3_0_0/guards/tests/run-guard-tests.sh
== resumo: 389 passaram, 0 falharam ==
SUÍTE VERDE — todos os casos-ruins BLOQUEADOS, todos os casos-bons passam.
EXIT:0
```

## Riscos residuais (honestidade)

1. Operador dono pode desmontar a jaula (limite JT-15 / spec) — processo/CODEOWNERS/Shield.
2. Exfiltração por leitura não coberta (desenho).
3. Spawn sem `core.hooksPath` pinado; credenciais de remote do repo pai não removidas do worktree.
4. Watchdog sem heartbeat temporal explícito.
5. Chokepoint Python é API voluntária pós-import.
6. Server-side (branch protection / CODEOWNERS / hbn-shield no GitHub) é requisito documentado, **não** exercido nesta auditoria local.
7. Não medi onboarding `spawn-orchestrator.sh` &lt;60s nem % de contexto do handoff.

## Confiança e o que NÃO verifiquei

**Confiança: alta (≈0.86)** no diff staged + baterias locais JT/anti-bypass/suíte 389, para os mecanismos de contenção em disco/hooks/wrapper/bypass estrutural.

**Não verifiquei:** proteção de branch no GitHub; CODEOWNERS efetivo; push real com keychain macOS fora do `jaula-sh`; launchd/cron do watchdog em produção; spawn end-to-end cronometrado; pareceres de outros auditores desta onda; criptografia ponta-a-ponta de hearbacks além do caminho JT-08/G-HRB já exercitado; conteúdo narrativo completo dos dois handoffs Codex além do necessário ao escopo.

Justificativa do veredito: o staged implementa contenção por mecanismo alinhada ao readback 0004/hearbacks; bypass estrutural fail-closed; JT-01..JT-14 e suíte 389/389 verdes sob execução deste auditor; resíduos acima são limites conhecidos ou lacunas operacionais que não reintroduzem bypass estrutural nem enfraquecem o endurecimento de CI da Onda-0b no pacote auditado.

APROVA_0004: SIM
