---
titulo: Parecer adversarial — RE-AUDITORIA delta readback 0002 (fechamento pós-auditoria v3.0.0)
tipo: result-cross-ia
status: ativo
temperatura: quente
path: .hbn/results/20260705-222303-antigravity-cross-ia-0002.md
created_at: "2026-07-05T22:23:03-03:00"
autor: antigravity
familia: Google
natureza: nativo
---
SOU: antigravity · familia Google · papel auditor

# Parecer adversarial — RE-AUDITORIA delta readback 0002 (fechamento pós-auditoria v3.0.0)

## Veredito
APROVA_0002: SIM

## Evidências (Truth Barrier)

### Pre-flight commands
- `pwd` -> `/Users/macbookpro/Projetos/usehbn`
- `git rev-parse --short HEAD` -> `a2eb6f2`
- `cat .hbn/active-version` -> `versao_3_0_0`
- `git status --short` -> ~19 mudanças em versao_3_0_0/ + shims de raiz
- `git diff --stat` -> delta de 12 arquivos modificados.

### Execuções obrigatórias
1. **Suíte de testes (run-guard-tests.sh)**:
   - `bash versao_3_0_0/guards/tests/run-guard-tests.sh`
   - Saída: `== resumo: 382 passaram, 0 falharam == SUÍTE VERDE` (staged handoff).
2. **Exúvia atômica dry-run (hbn-exuvia-atomic.sh)**:
   - `bash versao_3_0_0/scripts/hbn-exuvia-atomic.sh --dry-run --new versao_4_0_0`
   - Saída: `[hbn-exuvia-atomic] sandbox VERDE. DRY-RUN concluido: nada escrito no repo real.` (exit code: 0, executado em clone de verificação com a wave commitada).
3. **Upgrade de snapshot dry-run (hbn-upgrade-snapshot.sh)**:
   - `bash versao_3_0_0/scripts/hbn-upgrade-snapshot.sh --dry-run --target ~/Projetos/Credenciamento`
   - Saída: `manifesto: 85 arquivos hasheados`, `snapshot novo montado: 85 arquivos (source versao_3_0_0 @ a2eb6f2)` (exit code: 0).
4. **Bateria adversarial (adversarial-battery.sh)**:
   - `bash versao_3_0_0/guards/tests/adversarial-battery.sh`
   - Saída: `BATERIA VERDE — toda burla documentada foi BLOQUEADA pelo guard correspondente` (94/94 block, exit code: 0).

## Checklist T1/T3/T4/T6 + incidente §3

- **T1 — Exúvia atômica**:
  - A causa-raiz mecânica da suite-vermelha foi completamente solucionada.
  - O check `readlist-tracked` impede que referências existam apenas em arquivos untracked da working tree (o que faria o clone da exúvia nascer quebrado).
  - O sandbox foi movido para `$HOME` (evitando restrições de escrita em `/tmp/*` por parte do G-CR).
  - Em caso de falhas da suíte no sandbox, a saída não é mais silenciada (erros reportados no stderr e log salvo em `/tmp/hbn-exuvia-suite-*`).
- **T3 — Allowlist unificada**:
  - Alinhamento estrito nas 4 camadas: `assert-only-hot-version-writable.sh` (regra 4b), `.cursor/hooks/hbn-boot-lock.sh`, `AGENTS.md` e `BOOT.md` (§0.3).
  - Shims de raiz são considerados imutáveis em dev normal e exigem `hot-write-root-shim` assinado/confirmado em STATE/readback para alteração.
- **T4 — Higiene de quórum**:
  - Placeholders procedimentais antigos movidos para `.hbn/archive/PLACEHOLDER-*` e marcados como `status: placeholder-procedimental` (frios, não contam para quórum).
  - Pareceres reais mantidos no disco em `.hbn/results/` porém untracked, cumprindo a mecânica do G-DIVERSITY que impede commit de resultados sem consenso prévio.
  - `REGISTRY.md` alterado de forma puramente append-only.
- **T6 — Proteção anti-regressão**:
  - A regra 4c no G-HOT-WRITE bloqueia regressão do active-version para a raiz (`.`) sem exceção, mesmo que haja autorização confirmada.
- **Incidente §3 (Suite Leakage)**:
  - Adicionado `cd "${d:?}" || exit 1` em `run-guard-tests.sh` para garantir comportamento fail-closed da fixture `make_integrated_genesis_repo`, eliminando risco de execução no repo envolvente real.

## Furos encontrados (mesmo se SIM)

1. **Rito de exúvia exige commit para dry-run passar**: O script `hbn-exuvia-atomic.sh --dry-run` falha em working tree com modificações/novos arquivos untracked (ex.: o próprio handoff) porque ele efetua um `git clone` (que não herda dados não commitados), disparando o check `readlist-tracked` devido a referências a caminhos ainda não indexados em HEAD. Exige simulação em clone com commit ou pós-commit real.
2. **Suíte real exige staging**: A suíte `run-guard-tests.sh` executada diretamente no repositório real falha se novos arquivos referenciados estiverem untracked (deve-se rodar `git add` no arquivo antes de rodar os testes).
3. **Bypass residual**: A regra de bypass `HBN_GUARDS_BYPASS=1` com nota em `.hbn/bypasses/` ainda é aceita por ~32 guards não estruturais. A revogação total (proposta 0034) resta pendente de hearback humano.

## Confiança
Alta. Os cenários de regressão, shims e a exúvia atômica foram exaustivamente simulados em clone descartável e validados com sucesso por todas as ferramentas automáticas.

## NÃO verificado
- Execução no macOS real do operador (os testes foram executados e validados no workspace de desenvolvimento).
- Instalação real (`--install`) do snapshot no Credenciamento (apenas dry-run validado).
