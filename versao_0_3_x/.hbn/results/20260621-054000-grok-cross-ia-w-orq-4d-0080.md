---
path: .hbn/results/20260621-054000-grok-cross-ia-w-orq-4d-0080.md
id-global: 20260621-054000-grok-cross-ia-w-orq-4d-0080
tipo: audit-result
autor: grok
familia: xAI
veredito: "APROVA_0080: SIM"
arvore: fronteira
created_at: "2026-06-21T05:40:00-03:00"
status: congelado
temperatura: glacier
---

SOU: grok · familia xAI · papel auditor

VERIFICACOES OBRIGATORIAS (comando+saida + arquivo:linha):

1. git rev-parse main
   $ git rev-parse main
   4db692876381a0d7909985c8500d999f2e677b04
   → CONFIRMADO: main intocada (Truth Barrier).

2. .github/workflows/hbn-shield.yml — invocacoes + HBN_DIFF_BASE
   Linhas do blob HEAD (git cat-file -p HEAD:.github/workflows/hbn-shield.yml):
   ```
   26:      - uses: actions/checkout@v4
   27:        with:
   28:          fetch-depth: 0
   29:      - name: hbn-guards (modo CI — diff range, raiz canônica delegada ao local)
   30:        env:
   31:          HBN_DIFF_BASE: ${{ github.event.pull_request.base.sha || github.event.before }}
   32:        run: bash guards/hbn-guards-runner.sh
   33:      - name: hbn-guard-tests (suíte anti-teatro)
   34:        run: bash guards/tests/run-guard-tests.sh
   35:      - name: hbn-adversarial-battery
   36:        run: bash guards/tests/adversarial-battery.sh
   ```
   grep -n 'bash guards' HEAD blob:
   ```
   29:        run: bash guards/hbn-guards-runner.sh
   31:        run: bash guards/tests/run-guard-tests.sh
   33:        run: bash guards/tests/adversarial-battery.sh
   ```
   → ALEM do runner, agora roda run-guard-tests.sh (L31) e adversarial-battery.sh (L33).
   → HBN_DIFF_BASE preservado (L31 no blob).
   → YAML: estrutura valida (jobs.guards.steps com 3 runs no mesmo job/checkout).

3. guards/assert-ci-battery.sh — invariante (le blob do indice/HEAD)
   ```1:20:guards/assert-ci-battery.sh
   #!/usr/bin/env bash
   # G-CI-BATTERY: o HBN Shield deve rodar, no CI, a suite de guards e a
   # bateria adversarial. Guard invariante: le o workflow no indice local ou
   # em HEAD no CI, sem depender do diff, e falha fechado se a cobertura sumir.
   set -euo pipefail

   GUARD_NAME="assert-ci-battery"
   SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
   source "${SCRIPT_DIR}/lib/common.sh"

   if guard_check_bypass; then
       exit 0
   fi

   WORKFLOW_PATH=".github/workflows/hbn-shield.yml"

   blob_ref() {
       local repo_path
       repo_path="$(guard_version_repo_path "$WORKFLOW_PATH")" || return 1
       if [[ -n "${HBN_DIFF_BASE:-}" ]]; then
           printf 'HEAD:%s\n' "$repo_path"
       else
           printf ':%s\n' "$repo_path"
       fi
   }
   ...
   CONTENT="$(git cat-file -p "$WORKFLOW_REF" 2>/dev/null || true)"
   ...
   if ! grep -Eq '(^|[^A-Za-z0-9_./-])bash[[:space:]]+guards/tests/run-guard-tests\.sh([^A-Za-z0-9_./-]|$)' <<< "$ACTIVE_LINES"; then
       guard_fail "..."
   fi
   if ! grep -Eq '(^|[^A-Za-z0-9_./-])bash[[:space:]]+guards/tests/adversarial-battery\.sh([^A-Za-z0-9_./-]|$)' <<< "$ACTIVE_LINES"; then
       guard_fail "..."
   fi
   ```
   → Le `git cat-file` (HEAD quando HBN_DIFF_BASE, :path no indice local). Nunca working tree.
   → grep exato por "bash guards/tests/run-guard-tests.sh" e "bash guards/tests/adversarial-battery.sh".
   → Bloqueia (FAIL=1, exit 1) se faltar qualquer uma.

4. grep -n assert-ci-battery guards/hbn-guards-runner.sh
   ```
   110:    "assert-ci-battery.sh"
   ```
   → Guard presente no runner (ordem correta, apos assert-frontdoor).

5. bash guards/hbn-guards-runner.sh
   ```
   [hbn-guards/assert-ci-battery] ✓ CI preserva run-guard-tests.sh e adversarial-battery.sh no HBN Shield.
   ...
   [hbn-guards] Todos os guards passaram.
   ```
   → EXIT 0. G-CI-BATTERY incluido.

6. bash guards/tests/run-guard-tests.sh
   ```
   == assert-ci-battery (G-CI-BATTERY) ==
     ✓ ci-battery: workflow com suite+bateria passa (esperado: pass)
     ✓ ci-battery: workflow sem run-guard-tests.sh → BLOCK (esperado: block)
     ✓ ci-battery: workflow sem adversarial-battery.sh → BLOCK (esperado: block)
   ...
   == resumo: 253 passaram, 0 falharam ==
   SUÍTE VERDE — todos os casos-ruins BLOQUEADOS, todos os casos-bons passam.
   ```
   → 0 falharam.

7. bash guards/tests/adversarial-battery.sh
   ```
   B83 CI sem run-guard-tests.sh                        | G-CI     | BLOQUEADA ✓
   B84 CI sem adversarial-battery.sh                    | G-CI     | BLOQUEADA ✓

   BATERIA VERDE — toda burla documentada foi BLOQUEADA pelo guard correspondente.
   ```
   → B83 (workflow sem run-guard-tests) → BLOQUEADA.
   → B84 (workflow sem adversarial-battery) → BLOQUEADA.
   → BATERIA VERDE.

8. readback 0080
   ```
   "status": "implemented_pending_cross_audit",
   "activation_status": "PROPOSED_UNTIL_CROSS_AUDIT",
   ```
   Escopo files_allowed respeitado (10 artefatos listados). Despacho e readback referenciam corretamente.
   read-list-canonica, guards/data/*, core/orchestrator-profile-spec.md: NAO tocados (git diff --stat vazio para esses paths no commit 40f03d3 e no working tree atual).

9. Procura de burlas (ponto 8 da instrucao)
   - Guard passa com workflow SEM invocacao? NAO. Testes run-guard-tests + adversarial-battery (B83/B84) criam repos sinteticos e confirmam BLOCK. Guard le via git cat-file (HEAD/:), nao working-tree.
   - Workflow roda mas nao falha em regressao? NAO. Os passos no job guards sao sequenciais; se run-guard-tests ou adversarial falharem, o job falha (exit !=0).
   - Falso-verde via bypass? NAO. guard_check_bypass exige (env=1) + nota .hbn/bypasses/<ts>-<ag>-<mot>.md staged no diff (ou no range HBN_DIFF_BASE). Sem nota: "IGNORADO" + guard roda normalmente. Teste HBN_GUARDS_BYPASS=1 sem nota → guard passou so porque o workflow real esta correto.
   - Teste dependente do estado real do projeto? NAO. Casos B83/B84 usam mktemp + git init + fixture minima do workflow; guard e testado em isolamento. run-guard-tests cria fixtures descartaveis.
   - Nenhuma burla reproduzivel encontrada.

git rev-parse main == 4db692876381a0d7909985c8500d999f2e677b04 (INTOCADA).
Commit 40f03d3 altera apenas files_allowed do readback 0080.
Sem modificacoes em arquivos proibidos (core/**, guards/data/**, read-list, orchestrator-profile-spec).

VEREDICTO: implementacao de W-ORQ-4d (0080) esta correta, invariants mantidos, testes cobrem as burlas B83/B84, escopo respeitado, sem bypasses detectados.

APROVA_0080: SIM
