---
path: .hbn/results/20260618-131500-grok-cross-ia-w-orq-3-0061.md
id-global: 20260618-131500-grok-cross-ia-w-orq-3-0061
tipo: audit-result
autor: grok-cross-ia
familia: xAI
veredito: "APROVA_0061: SIM (cross-audit)"
arvore: fronteira
created_at: "2026-06-18T13:15:00-03:00"
status: congelado
temperatura: glacier
---

SOU: grok-cross-ia · familia xAI · papel auditor

# CROSS-AUDIT useHBN — W-ORQ-3 / G-ORQ-REF (readback 0061) · APROVA_0061

**PARA:** grok (xAI) — auditor cross ≠-OpenAI (implementador codex OpenAI; orquestrador opus-4-8 Anthropic). 2a familia != OpenAI requerida.
**ALVO:** commit 3da43c4 (feat(hbn): add orq entrada ref guard) + .hbn/readbacks/0061-w-orq-3.json + guards/assert-orq-entrada-ref.sh + bateria B48-B51 + runner.
**REGRA:** Truth Barrier — tudo lido do disco (git show no commit, arquivos reais, execucao de testes). READ-ONLY. Sem stage adicional, sem --no-verify, sem tocar main.

---

Veredito registrado no front-matter e repetido ao final como linha isolada.

---

## 1. Verificações (no disco + commit 3da43c4)

### 1. main invariant (do readback 0061)
- git rev-parse main → 4db692876381a0d7909985c8500d999f2e677b04
- Commit 3da43c4 preserva (HEAD atual do branch de entrega é o commit da onda).
- **OK.**

### 2. Escopo exato do commit (9 arquivos = files_allowed do readback)
- git show --stat 3da43c4
  - .hbn/attestations/34a7f2f9-orq-entrada.json
  - .hbn/messages/20260618-121842-codex-handoff-w-orq-3.md
  - .hbn/readbacks/0061-w-orq-3.json
  - .hbn/relay/STATE.md
  - REGISTRY.md
  - guards/assert-orq-entrada-ref.sh (novo, 272 linhas)
  - guards/hbn-guards-runner.sh (+1)
  - guards/tests/adversarial-battery.sh (+ B48-B51)
  - guards/tests/run-guard-tests.sh (+6 checks G-ORQ-REF)
- git show 3da43c4:.hbn/readbacks/0061-w-orq-3.json → scope.files_allowed exato (9 itens), status=in_progress (não selagem), orq_entrada_ref presente.
- **Escopo respeitado. Entrega não-autoridade isenta do gate (como projetado).** **OK.**

### 3. Guard G-ORQ-REF (assert-orq-entrada-ref.sh) — fail-closed real
- Gateia SOMENTE atos de autoridade do orquestrador:
  - .hbn/dispatch/*.md (dereferencia readback_id)
  - .hbn/freeze/*.json
  - .hbn/readbacks/* com marcadores authority_act/ato_autoridade=selagem ou status selado/vigente ou nome contendo selagem.
- Atos não-autoridade (entregas, pareceres, readback 0061) → AUTH 0 → exit 0 sem opinar.
- Requer orq_entrada_ref == .hbn/attestations/<fp8>-orq-entrada.json derivado de bastao_token_sha256[:8] do STATE.
- Verifica existência no índice/HEAD (blob_exists via git cat-file).
- Bloqueia FP divergente, ref omitido, JSON ausente/ilegível, ref != esperado.
- Re-pin detecção:
  - local: changed_attestation + !HBN_DIFF_BASE → BLOCK
  - CI: rev-list HBN_DIFF_BASE..HEAD + mistura auth+repin → BLOCK
- **Fail-closed verificado.** **OK.**

### 4. Reuso de assert-orq-entrada.sh (não reimplementa validação)
- No final do guard:
  ```
  if ! bash "${SCRIPT_DIR}/assert-orq-entrada.sh"; then
      guard_fail "Atestacao referenciada por orq_entrada_ref nao passa o assert-orq-entrada.sh vigente (${EXPECTED_REF})."
  ```
- Delegação explícita para o guard v2 vigente (extrativo, manifest_sha, line/field responses, 13 itens da read-list canonica).
- **Reuso confirmado, sem duplicação de lógica.** **OK.**

### 5. B48-B51 + bateria adversarial
- Casos no disco (guards/tests/adversarial-battery.sh:958+):
  - B48: despacho com orq_entrada_ref omitido → BLOQUEADA ✓ (G-ORQREF)
  - B49: orq_entrada_ref dangling/ausente (rm da atestacao) → BLOQUEADA ✓
  - B50: orq_entrada_ref com fp trocado (deadbeef) → BLOQUEADA ✓
  - B51: auto-repin no mesmo commit de ato de autoridade (edit + add atestacao junto com dispatch) → BLOQUEADA ✓
- Execução real:
  ```
  bash guards/tests/adversarial-battery.sh
  ...
  B48 ... | G-ORQREF | BLOQUEADA ✓
  B49 ... | G-ORQREF | BLOQUEADA ✓
  B50 ... | G-ORQREF | BLOQUEADA ✓
  B51 ... | G-ORQREF | BLOQUEADA ✓
  BATERIA VERDE — toda burla documentada foi BLOQUEADA
  ```
- **Ausência de burla confirmada para os 4 vetores de W-ORQ-3.** **OK.**

### 6. Testes run-guard-tests + runner
- bash guards/tests/run-guard-tests.sh → 208 passaram, 0 falharam (inclui +6 para G-ORQ-REF: 1 pass + 4 block + isenção não-autoridade).
- bash guards/hbn-guards-runner.sh → todos guards OK, inclusive:
  ```
  [hbn-guards/assert-orq-entrada-ref] ✓ Nenhum ato de autoridade do orquestrador staged/HEAD — G-ORQ-REF nao opina.
  [hbn-guards/assert-orq-entrada] ✓ Atestacao de entrada v2 valida...
  [hbn-guards] Todos os guards passaram.
  ```
- Atestacao no commit 3da43c4 valida (manifest, 3 linhas + 2 fields, fp 34a7f2f9).
- **Evidencia mecanica reprodutivel no disco.** **OK.**

### 7. Invariantes e escopo preservados
- Sem alteração em core/**, schemas/**, src/**, methodology/**, docs/brainstorm/**, .hbn/freeze/**.
- main não tocado.
- G-EXC (F-01) permanece visível e rastreável (implementador=codex == agente do readback 0061).
- Trailers HBN no handoff do commit.
- Dogfood: entrega do guard não é ato de autoridade (status in_progress) → isento, como especificado.
- **Escopo e fail-closed por construção.** **OK.**

---

## JUIZO

W-ORQ-3 implementa G-ORQ-REF como gate aditivo fail-closed estrito para atos de autoridade do orquestrador. 

- Reuso correto de assert-orq-entrada.sh.
- 4 casos de fail-closed cobertos por B48-B51 e bloqueados na bateria.
- Escopo preciso (somente autoridade; entregas/pareceres isentos).
- Testes 208/208 + runner verde + adversarial B1-B51 verde.
- Sem burla, sem fresta mecânica nos vetores atacados, sem poluição de escopo.

Nenhuma severidade bloqueadora ou residual para o objetivo declarado.

---

**Evidencias mecanicas (arquivo:linha ou cmd+saida no disco):**
- git show 3da43c4 --stat
- git show 3da43c4:.hbn/readbacks/0061-w-orq-3.json (orq_entrada_ref + status + scope)
- git show 3da43c4:guards/assert-orq-entrada-ref.sh (reuso + fail cases)
- grep -n 'B4[8-9]\|B5[0-1]' guards/tests/adversarial-battery.sh
- bash guards/tests/run-guard-tests.sh (208/0)
- bash guards/tests/adversarial-battery.sh (B48-B51 BLOQUEADAS + BATERIA VERDE)
- bash guards/hbn-guards-runner.sh
- git rev-parse main
- git cat-file na atestacao 34a7f2f9

---

APROVA_0061: SIM

Fim do parecer cross-audit grok/xAI (W-ORQ-3 / 0061).