---
path: .hbn/results/20260618-184500-grok-cross-ia-w-orq-3b-0062.md
id-global: 20260618-184500-grok-cross-ia-w-orq-3b-0062
tipo: audit-result
autor: grok
familia: xAI
veredito: "APROVA_0062: SIM"
arvore: fronteira
created_at: "2026-06-18T18:45:00-03:00"
status: congelado
temperatura: glacier
---

SOU: grok · familia xAI · papel auditor

(corpo: verificações executadas no disco, arquivo:linha / comando+saída)

## Verificações de Verdade (Truth Barrier — tudo do disco, git show e execução real)

### 1. Estado Git exato (disco)
Comando executado:
```bash
cd /Users/macbookpro/Projetos/usehbn && \
git rev-parse main && \
git rev-parse HEAD
```
Saída:
```
4db692876381a0d7909985c8500d999f2e677b04
426fdd76f7d56c177e0709737a97e844b008abeb
```

Verificação cruzada:
```bash
git rev-parse main | grep -c '^4db692876381a0d7909985c8500d999f2e677b04$'  → 1
git rev-parse HEAD | grep -c '^426fdd76f7d56c177e0709737a97e844b008abeb$'  → 1
```
**main intacto em 4db692876381a0d7909985c8500d999f2e677b04** ✓
**HEAD no commit da onda 426fdd76f7d56c177e0709737a97e844b008abeb** ✓

### 2. Escopo exato do readback 0062 (git show no commit)
```bash
git show 426fdd76f7d56c177e0709737a97e844b008abeb:.hbn/readbacks/0062-w-orq-3b.json | \
  python3 -c '...extrair scope.files_allowed...'
```
files_allowed (idêntico ao declarado):
```
[
  ".hbn/readbacks/0062-w-orq-3b.json",
  ".hbn/messages/20260618-134916-opus-4-8-despacho-w-orq-3b.md",
  "guards/assert-orq-entrada-ref.sh",
  "guards/assert-orq-entrada.sh",
  "guards/lib/common.sh",
  "guards/tests/run-guard-tests.sh",
  "guards/tests/adversarial-battery.sh",
  ".hbn/relay/STATE.md",
  "REGISTRY.md",
  ".hbn/messages/20260618-153000-codex-handoff-w-orq-3b.md",
  ".hbn/attestations/34a7f2f9-orq-entrada.json"
]
```
**ESCOPO RESPEITADO** — commit tocou exatamente estes + artefatos de entrega dentro da lista.

### 3. Lógica Exit A' no guard (git show + leitura de fonte no commit)
Arquivo: guards/assert-orq-entrada-ref.sh (commit 426fdd7)

Trechos críticos (linhas do git show):

```python
# valida apenas MODIFICAÇÃO da atestação esperada
if entry.get("status") != "M" or entry.get("old") is not None or entry.get("new") != expected_ref:
    fail("ato de autoridade so pode MODIFICAR a atestacao esperada ...")

if any(path != expected_ref for path in changed_att_paths):
    fail("atestacao fora do fp esperado alterada em ato de autoridade")

# full SHA do bastão não pode mudar
if old_token_sha and token_sha and old_token_sha != token_sha:
    fail("bastao_token_sha256 completo mudou no ato de autoridade")

# regeneração REAL: manifest deve mudar
if old_data.get("manifest_sha256") == new_data.get("manifest_sha256"):
    fail("regeneracao de atestacao nao alterou manifest_sha256")

# proveniência
if old_data.get("proprietario_bastao") != new_data.get("proprietario_bastao"):
    fail("proprietario_bastao diverge do HEAD/base")
if old_data.get("identidade") != new_data.get("identidade"):
    fail("identidade diverge do HEAD/base")

# atestação nova deve passar G-ORQ-ENTRADA
result = subprocess.run( ["bash", ... "assert-orq-entrada.sh"], ... )
if result.returncode != 0:
    fail("nova atestacao nao passa assert-orq-entrada.sh")
```

- Só permite em contexto de autoridade (despacho/selagem/freeze detectados via is_authority_readback + dispatch).
- Modo local (index vs HEAD) e CI (HBN_DIFF_BASE, commit^ vs commit) são simétricos.
- Qualquer desvio (fp, SHA completo, manifest igual, múltiplas atestações, atestação extra, proveniência divergente, orq_entrada_ref divergente) → fail + sys.exit(1).
**Exit A' implementado fail-closed conforme especificação** ✓

### 4. Execução real dos testes (no disco, sem confiar em relatos)

Comando 1:
```bash
bash guards/tests/run-guard-tests.sh
```
Resultado (tail real da execução):
```
== assert-orq-entrada-ref (G-ORQ-REF) ==
  ✓ orq-ref: despacho com ref valida passa (esperado: pass)
  ✓ orq-ref: despacho com ref omitido → BLOCK (esperado: block)
  ✓ orq-ref: ref dangling/atestacao ausente → BLOCK (esperado: block)
  ✓ orq-ref: fp trocado no ref → BLOCK (esperado: block)
  ✓ orq-ref: auto-repin no mesmo commit → BLOCK (esperado: block)
  ✓ orq-ref: entrega nao-autoridade sem ref passa (esperado: pass)
  ✓ orq-ref P1: selagem real same-fp passa G-ORQ + G-ORQ-REF (esperado: pass)
  ✓ orq-ref N1: selagem sem regenerar atestacao → BLOCK (esperado: block)
  ✓ orq-ref N2: atestacao decorativa sem manifest novo → BLOCK (esperado: block)
  ✓ orq-ref N3: atestacao esperada com fp JSON trocado → BLOCK (esperado: block)
  ✓ orq-ref N4: atestacao extra staged junto → BLOCK (esperado: block)
  ✓ orq-ref N5: bastao_token_sha256 completo trocado com mesmo fp → BLOCK (esperado: block)
  ✓ orq-ref N6: identidade divergente do HEAD/base → BLOCK (esperado: block)
  ✓ orq-ref CI: selagem same-fp passa no range (esperado: pass)
  ✓ orq-ref CI: fp trocado bloqueia no range (esperado: block)

== resumo: 217 passaram, 0 falharam ==
SUÍTE VERDE — todos os casos-ruins BLOQUEADOS, todos os casos-bons passam.
exit_code: 0
```
**0 falharam (P1 + N1-N6 + CI cases inclusos)** ✓

Comando 2:
```bash
bash guards/tests/adversarial-battery.sh
```
Resultado (tail real):
```
B48 despacho com orq_entrada_ref omitido             | G-ORQREF | BLOQUEADA ✓
B49 orq_entrada_ref dangling/ausente                 | G-ORQREF | BLOQUEADA ✓
B50 orq_entrada_ref com fp trocado                   | G-ORQREF | BLOQUEADA ✓
B51 auto-repin sem regeneracao real                  | G-ORQREF | BLOQUEADA ✓
B52 atestacao esperada com fp JSON trocado           | G-ORQREF | BLOQUEADA ✓
B53 full-SHA do bastao trocado com mesmo fp          | G-ORQREF | BLOQUEADA ✓
B54 atestacao extra staged junto ao ato              | G-ORQREF | BLOQUEADA ✓

BATERIA VERDE — toda burla documentada foi BLOQUEADA pelo guard correspondente.
exit_code: 0
```
**BATERIA VERDE (B48-B54)** ✓

### 5. Atestação e STATE no commit auditado (git show)
```bash
git show 426fdd7:.hbn/attestations/34a7f2f9-orq-entrada.json
```
```
bastao_token_fp: 34a7f2f9
manifest_sha256: ec8625c2629ed874...
proprietario_bastao: claude-opus-4-8
identidade: opus-4-8
execution_id: w-orq-3b-2026-06-18
algorithm: orq-entrada.v2/extractive-lines
```
STATE no commit continha bastao_token_sha256 completo correto e sinais documentando "W-ORQ-3b ENTREGUE — aguardando cross-audit grok/xAI + antigravity".

Despacho e handoff presentes no commit com orq_entrada_ref declarado.

### 6. main permaneceu intacto durante toda a entrega
Confirmado por git rev-parse no momento da auditoria (após checkout do HEAD da branch de proposta). Nenhuma alteração em main.

## Veredito

Todas as verificações de Truth Barrier passaram:
- git refs exatos
- escopo respeitado
- lógica Exit A' fail-closed contra auto-repin / troca bastão / fp trocado / full-SHA mesmo prefixo / atestação extra / proveniência divergente
- suite: 217/217, 0 falharam (P1+N1-N6+CI)
- bateria adversarial: B48-B54 todas BLOQUEADAS
- main intacto em 4db6928...

APROVA_0062: SIM
