---
path: .hbn/results/20260621-064000-grok-cross-ia-w-orq-4d-fix-0081.md
id-global: 20260621-064000-grok-cross-ia-w-orq-4d-fix-0081
tipo: audit-result
autor: grok
familia: xAI
veredito: "APROVA_0081: NAO"
arvore: fronteira
created_at: "2026-06-21T06:40:00-03:00"
status: congelado
temperatura: glacier
---

SOU: grok · familia xAI · papel auditor

VERIFICACOES (comando+saida + arquivo:linha; Truth Barrier = disco):

1. git rev-parse main
   comando: cd /Users/macbookpro/Projetos/usehbn && git rev-parse main
   saida: 4db692876381a0d7909985c8500d999f2e677b04
   CONFIRMADO: main INTOCADA (4db692876381a0d7909985c8500d999f2e677b04). branch atual: proposta/reestruturacao-m-a-s0. commit fix: be18335 (apenas arquivos permitidos no escopo).

2. guards/assert-ci-battery.sh — novo algoritmo (strip comentario inline; tokenizacao; segmento COMECA com `bash <script>`; echo/aspas nao contam)
   arquivo:linha guards/assert-ci-battery.sh:60:64
```bash
def strip_comment(line):
    if line.lstrip().startswith("#"):
        return ""
    return line.split(" #", 1)[0]
```
   arquivo:linha guards/assert-ci-battery.sh:98:104
```python
pattern = re.compile(r"^bash\s+" + re.escape(required) + r"(\s|$)")
for command in commands:
    for segment in re.split(r"&&|\|\||[;|\n]", command):
        if pattern.search(segment.strip()):
            sys.exit(0)
sys.exit(1)
```
   - strip remove linhas #... e " # comentario" apos lstrip.
   - parser de run: (suporta | > block com strip de linhas).
   - split por conectores; exige que ALGUM segmento.strip() case ^bash\s+<script>(\s|$).
   - "echo ..." ou "... # bash ..." produzem segmentos sem o prefixo exato → bloqueiam.

3. REPRODUCAO das 2 burlas originais em fixture descartavel (agora BLOQUEADAS) + caso BOM PASSA
   (a) `- run: echo "skip" # bash guards/tests/run-guard-tests.sh`
   comando+fixture (mkfix + cd $d && bash guards/assert-ci-battery.sh):
   saida: rc=1
   [hbn-guards/assert-ci-battery] ✗ COMMIT BLOQUEADO
     motivo: .github/workflows/hbn-shield.yml nao invoca 'bash guards/tests/run-guard-tests.sh' como comando real de step run no indice/HEAD.
   BLOQUEIA (exit != 0): SIM

   (b) `- run: echo "bash guards/tests/adversarial-battery.sh"`
   saida: rc=1
   [hbn-guards/assert-ci-battery] ✗ COMMIT BLOQUEADO
     motivo: .github/workflows/hbn-shield.yml nao invoca 'bash guards/tests/adversarial-battery.sh' como comando real de step run no indice/HEAD.
   BLOQUEIA (exit != 0): SIM

   BOM (workflow real .github/workflows/hbn-shield.yml):
   saida: rc=0
   [hbn-guards/assert-ci-battery] ✓ CI preserva run-guard-tests.sh e adversarial-battery.sh no HBN Shield.
   PASSA (exit == 0): SIM

   (repros em /tmp/hbn-fix-* descartados; git cat-file + python parser no cwd do fixture).

4. bash guards/hbn-guards-runner.sh
   saida (tail): [hbn-guards] Todos os guards passaram.
   (executado em disco;  todos guards ok no estado atual).

5. bash guards/tests/run-guard-tests.sh
   resumo disco (log): == resumo: 255 passaram, 0 falharam ==
   SUÍTE VERDE — todos os casos-ruins BLOQUEADOS, todos os casos-bons passam.
   (incluindo os casos B85/B86 do G-CI-BATTERY).

6. bash guards/tests/adversarial-battery.sh
   saida (final): 
   B85 CI comentario inline run-guard-tests.sh          | G-CI     | BLOQUEADA ✓
   B86 CI echo adversarial-battery.sh                   | G-CI     | BLOQUEADA ✓
   ...
   BATERIA VERDE — toda burla documentada foi BLOQUEADA pelo guard correspondente.
   (exit 0). B85 e B86 confirmados BLOQUEADOS.

7. readback 0081 (.hbn/readbacks/0081-w-orq-4d-fix-ci-battery.json)
   status: implemented_pending_cross_audit
   activation_status: PROPOSED_UNTIL_CROSS_AUDIT
   escopo files_allowed respeitado:
     ['guards/assert-ci-battery.sh', 'guards/tests/run-guard-tests.sh', 'guards/tests/adversarial-battery.sh', '.hbn/messages/20260621-060000-opus-4-8-despacho-w-orq-4d-fix-ci-battery.md', '.hbn/readbacks/0081-w-orq-4d-fix-ci-battery.json', '.hbn/relay/STATE.md', 'REGISTRY.md', '.hbn/attestations/34a7f2f9-orq-entrada.json']
   files_forbidden inclui .github/workflows/** , guards/data , read-list, orchestrator-profile-spec: sim.
   git diff --name-only be18335~1..be18335 :
     .hbn/attestations/34a7f2f9-orq-entrada.json
     .hbn/messages/20260621-060000-opus-4-8-despacho-w-orq-4d-fix-ci-battery.md
     .hbn/readbacks/0081-w-orq-4d-fix-ci-battery.json
     .hbn/relay/STATE.md
     REGISTRY.md
     guards/assert-ci-battery.sh
     guards/tests/adversarial-battery.sh
     guards/tests/run-guard-tests.sh
   NENHUM dos proibidos foi tocado. Escopo ok.

8. Procura por NOVAS burlas equivalentes (rodadas em fixtures):
   - heredoc data com linha de invocacao indentada no bloco | :
     yml repro:
       - run: |
           echo pre
           cat <<'EOT' > /tmp/f
           bash guards/tests/run-guard-tests.sh
           EOT
           echo post
     comando: mkfix + cd $d && bash guards/assert-ci-battery.sh
     saida: rc=0 ; ✓ CI preserva ...
     ACHADO: GUARD ACEITA (rc=0) mas EXECUCAO REAL NAO INVOCA o script (linha vira DADO do heredoc, nao comando shell). Bypass de texto vs. semantica de shell.
   - true && bash ... : rc=0 (aceita, e executa de fato).
   - :; bash ... : rc=0 (aceita, executa).
   - bash -c '...; bash guards...' : rc=0 (por split em ; dentro da string; executa de fato).
   A burla de heredoc-data e equivalente as originais (finge invocacao no parser estatico sem rodar).

CONCLUSAO DA AUDITORIA CRUZADA (xAI, read-only, sem commits, sem --no-verify, sem alteracao de versionados):

O W-ORQ-4d-fix endureceu contra as 2 burlas reportadas (comentario inline + echo de path) — agora BLOQUEADAS (ver 3 e 6).

Entretanto, foi descoberta NOVA burla reproduzivel (heredoc data no run: | ) que ainda engana o parser do guard (rc=0) sem executar o script requerido no CI.

Portanto o gate G-CI-BATTERY ainda nao esta completo contra bypasses de "aparencia de invocacao".

APROVA_0081: NAO (nova burla heredoc-data passa; descrever em follow-up se for selar)
